
2. PROBLÈMES CRITIQUES (À CORRIGER IMMÉDIATEMENT)
================================================================================

┌──────────────────────────────────────────────────────────────────────────┐
│ CRITIQUE #1: LEAK MÉMOIRE MASSIF - Client Java                          │
└──────────────────────────────────────────────────────────────────────────┘

Fichier: java-client/client-app/src/main/java/com/lemondelila/client/
         game/core/service/SseLogger.java

Evidence: logsse.txt contient 627 lignes IDENTIQUES:
  SseLogger initialisé (C:\wamp64\www\LemondeDeLila\java-client\logsse.txt)
  SseLogger initialisé (C:\wamp64\www\LemondeDeLila\java-client\logsse.txt)
  ... ×627

Impact:
  - 626 instances orphelines de SseLogger
  - 626 threads de logging potentiellement actifs
  - Fuite mémoire progressive
  - Ralentissement du client

Cause:
  SseLogger est instantié à chaque utilisation au lieu d'être un singleton

Code problématique (lignes 47-54):
  private void writeLine(String text) {
      try {
          Files.writeString(file, text + System.lineSeparator(),
                  StandardOpenOption.CREATE,
                  StandardOpenOption.APPEND);  // ← SANS SYNCHRONISATION
      } catch (IOException e) {
          System.err.println("Impossible d'écrire dans " + file + ": " +
                             e.getMessage());
      }
  }

Problèmes:
  1. Non thread-safe (plusieurs threads écrivent simultanément)
  2. Pas de buffering (IO bloquant sur chaque appel)
  3. 627 instances créées au lieu d'une seule

Solution:
  Implémenter un Singleton pattern:

  public class SseLogger {
      private static final SseLogger INSTANCE = new SseLogger();
      private final Object lock = new Object();

      private SseLogger() { /* ... */ }

      public static SseLogger getInstance() {
          return INSTANCE;
      }

      public void log(String text) {
          synchronized (lock) {
              writeLine(text);
          }
      }
  }


┌──────────────────────────────────────────────────────────────────────────┐
│ CRITIQUE #2: RACE CONDITION EDT - Client Java                           │
└──────────────────────────────────────────────────────────────────────────┘

Fichier: java-client/client-app/src/main/java/com/lemondelila/client/
         game/core/view/GenericGameInteractionComponent.java

Lignes: 476-478

Code problématique:
  @Override
  public void onState(GenericGameState state) {
      this.lastState = state;  // ← ASSIGNATION HORS EDT !
      SwingUtilities.invokeLater(() -> {
          renderState(state);  // ← LECTURE DANS EDT
      });
  }

Impact:
  - Race condition entre thread réseau et EDT
  - Corruption possible de l'état de l'interface
  - Crash potentiel ou affichage incohérent
  - Violations du threading model Swing

Scénario problématique:
  Thread 1 (Network): lastState = state1
  Thread 2 (EDT):     renderState(state1) commence
  Thread 1 (Network): lastState = state2  // ← state2 écrase state1
  Thread 2 (EDT):     lit lastState → obtient state2 au lieu de state1

Solution:
  @Override
  public void onState(GenericGameState state) {
      SwingUtilities.invokeLater(() -> {
          this.lastState = state;  // ← DANS l'EDT
          renderState(state);
      });
  }

Aussi dans onError (lignes 482-490):
  Même problème, même solution


┌──────────────────────────────────────────────────────────────────────────┐
│ CRITIQUE #3: ABSENCE DE PERSISTANCE - Backend                           │
└──────────────────────────────────────────────────────────────────────────┘

Fichier: backend/src/game/engine/services/game-engine-state.store.ts

Code problématique (lignes 7-23):
  @Injectable()
  export class GameEngineStateStore {
    private readonly states = new Map<string, GameStateEntity>();
    // ← Stockage EN MÉMOIRE uniquement !

    get(roomId: number, gameType: string): GameStateEntity | undefined {
      return this.states.get(this.buildKey(roomId, gameType));
    }

    set(roomId: number, gameType: string, state: GameStateEntity): void {
      this.states.set(this.buildKey(roomId, gameType), state);
    }
  }

Impact:
  - Si le serveur redémarre → TOUTES les parties en cours sont perdues
  - Aucune sauvegarde automatique
  - Aucune récupération possible
  - Frustration des joueurs
  - Perte de données métier importantes

Problèmes supplémentaires:
  - Pas d'expiration des états (fuite mémoire à long terme)
  - Pas de limite de taille
  - Pas de stratégie de cache

Solution immédiate:
  1. Ajouter TypeORM pour persistence
  2. Créer une entité GameState
  3. Implémenter un Repository pattern

  @Entity()
  export class GameStateEntity {
    @PrimaryColumn()
    id: string;  // roomId:gameType

    @Column('json')
    state: any;

    @UpdateDateColumn()
    updatedAt: Date;
  }

  @Injectable()
  export class GameStateRepository {
    constructor(
      @InjectRepository(GameStateEntity)
      private repo: Repository<GameStateEntity>
    ) {}

    async save(roomId: number, gameType: string, state: any) {
      await this.repo.save({ id: `${roomId}:${gameType}`, state });
    }

    async load(roomId: number, gameType: string) {
      const entity = await this.repo.findOne({
        where: { id: `${roomId}:${gameType}` }
      });
      return entity?.state;
    }
  }

Ou alternative Redis pour performance:
  @Injectable()
  export class GameStateCache {
    constructor(@InjectRedis() private redis: Redis) {}

    async save(roomId: number, gameType: string, state: any) {
      const key = `game:${roomId}:${gameType}`;
      await this.redis.setex(key, 3600, JSON.stringify(state));
    }
  }


┌──────────────────────────────────────────────────────────────────────────┐
│ CRITIQUE #4: VIOLATION SRP - PanierExpressService                       │
└──────────────────────────────────────────────────────────────────────────┘

Fichier: backend/src/game/games/jeux-de-plateaux/les-quatre-vents/
         panier-express/services/panier-express.service.ts

Taille: 1040 LIGNES (!!!)

Dépendances: 19 SERVICES injectés:
  export class PanierExpressService implements GameRulesAdapter, OnModuleInit {
    constructor(
      private readonly core: GameCoreService,
      private readonly turns: TurnService,
      private readonly deckPool: DeckPoolService,
      private readonly movement: BoardMovementService,
      private readonly tileRegistry: TileEffectRegistryService,
      private readonly turnActions: TurnActionsService,
      private readonly standEffects: StandEffectRegistryService,
      private readonly resolver: ActionResolverService,
      private readonly turnStatus: TurnStatusService,
      private readonly victory: VictoryService,
      private readonly botRunner: BotRunnerService,
      private readonly actionLogSvc: ActionLogService,
      private readonly registry: GameRegistryService,
      private readonly setup: PanierExpressSetupService,
      private readonly drawSvc: PanierExpressDrawService,
      private readonly quizSvc: PanierExpressQuizService,
      private readonly quizRunner: QuizRunnerService,
      private readonly exchangeHelper: GenericExchangeService,
      private readonly exchangeSvc: PanierExpressExchangeService,
      private readonly utils: PanierExpressUtils,
    ) {}

Responsabilités (trop nombreuses):
  1. Exposer l'état (exposeState - 139 lignes)
  2. Initialiser le jeu (hydrateInitialState - 74 lignes)
  3. Appliquer les actions (applyActions + dispatchAction - 29 lignes)
  4. Gérer les bots (getBotActions - 54 lignes)
  5. Lister les actions disponibles (getAvailableActions - 37 lignes)
  6. Construire/fusionner métadonnées (buildMetadata + merge - 30 lignes)
  7. Gérer les tours (advanceTurn - 32 lignes)
  8. Appliquer victoire (applyVictory - 16 lignes)
  9. Gérer le lancer de dé (handleRoll - 32 lignes)
  10. Gérer les quiz (handleAnswerQuiz - 38 lignes)
  11. Gérer les échanges (handleExchangeWith - 17 lignes)
  12. Déplacer les joueurs (movePlayer - 18 lignes)
  13. Résoudre les tuiles (resolveTile - 13 lignes)
  14. Enregistrer les handlers (registerTileHandlers - 19 lignes)
  15. Appliquer événements (applyEvent - 44 lignes)
  16. Formater des labels (tileLabel, standLabel - 35 lignes)
  17. Normaliser les données (toStringArray, etc. - 40+ lignes)
  18. Et plus...

Impact:
  - Impossible à tester unitairement (mock 19 services)
  - Maintenance cauchemardesque
  - Violation flagrante du Single Responsibility Principle
  - Couplage fort avec tous les services du jeu
  - Réutilisation impossible
  - Modifications risquées (effet domino)

Métriques de complexité:
  - Nombre de méthodes privées: 30+
  - Nombre de méthodes publiques: 10+
  - Cyclomatic complexity: TRÈS ÉLEVÉE
  - Testabilité: TRÈS FAIBLE

Solution - Décomposer en 5 services:

  1. PanierExpressOrchestrator
     - Coordination générale
     - applyActions, dispatchAction
     - Responsabilité: orchestrer le flux du jeu

  2. PanierExpressStatePresenter
     - exposeState
     - buildView, buildPendingView, buildPlayerViews
     - Responsabilité: formatter l'état pour le client

  3. PanierExpressStateInitializer
     - hydrateInitialState
     - ensureMetadata, buildMetadata
     - Responsabilité: initialiser les parties

  4. PanierExpressBotStrategy
     - getBotActions
     - injectQuizAnswer
     - Responsabilité: IA des bots

  5. PanierExpressActionHandler
     - handleRoll, handleAnswerQuiz, handleExchangeWith
     - getAvailableActions
     - Responsabilité: gérer les actions joueurs

  Avec ces services auxiliaires existants:
     - PanierExpressDrawService (déjà existant)
     - PanierExpressSetupService (déjà existant)
     - PanierExpressExchangeService (déjà existant)
     - PanierExpressQuizService (déjà existant)
     - PanierExpressUtils (déjà existant)


┌──────────────────────────────────────────────────────────────────────────┐
│ CRITIQUE #5: SÉCURITÉ - Clé de chiffrement sur disque                   │
└──────────────────────────────────────────────────────────────────────────┘

Fichier: java-client/client-app/src/main/java/com/lemondelila/client/
         security/EncryptedSessionVault.java

Code problématique:
  public static EncryptedSessionVault defaultVault() {
      Path directory = Path.of("config");
      return new EncryptedSessionVault(
          directory.resolve("session.bin"),     // ← Données chiffrées
          directory.resolve(".session.key")     // ← CLÉ EN CLAIR sur disque
      );
  }

Impact:
  - Attaquant local peut lire .session.key
  - Déchiffrer session.bin avec la clé
  - Accès aux credentials stockés
  - Usurpation d'identité possible

Le chiffrement AES/GCM est correct:
  private static final String TRANSFORMATION = "AES/GCM/NoPadding";
  private static final int GCM_TAG_LENGTH = 128;
  private static final int IV_LENGTH = 12;

MAIS la clé est stockée en clair sur le disque.

Solutions possibles:

  Option 1: Dériver la clé d'un mot de passe utilisateur
    - Demander mot de passe au démarrage
    - Utiliser PBKDF2 pour dériver la clé
    - Ne jamais stocker la clé

  Option 2: Utiliser le keystore système
    - Windows: DPAPI (Data Protection API)
    - Linux: libsecret / gnome-keyring
    - macOS: Keychain

  Option 3: Hybrid (recommandé)
    - Clé maître dans keystore système
    - Clé de session dérivée
    - Rotation périodique


================================================================================
3. PROBLÈMES ÉLEVÉS
================================================================================

┌──────────────────────────────────────────────────────────────────────────┐
│ ÉLEVÉ #1: GOD COMPONENT - GenericGameInteractionComponent               │
└──────────────────────────────────────────────────────────────────────────┘

Fichier: GenericGameInteractionComponent.java

Taille: 1032 lignes

Champs: 70+ champs privés:
  private GenericGameState lastState;
  private List<String> lastSelfShopping = List.of();
  private List<String> lastSelfBasket = List.of();
  private List<String> lastSelfInventory = List.of();
  private List<String> shoppingView = List.of();
  private List<String> basketView = List.of();
  private List<String> inventoryView = List.of();
  private List<Map<String, Object>> dynamicShortcuts = List.of();
  private Integer lastRollSeen;
  private boolean exchangePending;
  private boolean gameStarted;
  private boolean startAnnounced;
  private boolean firstStateRendered;
  private boolean autoPrimaryDispatched;
  // ... 56 autres champs

Responsabilités:
  1. Gérer l'état du jeu (lastState, etc.)
  2. Construire l'interface utilisateur (panels, buttons, etc.)
  3. Gérer les raccourcis clavier (70+ bindings)
  4. Gérer les dialogs (quiz, exchange, etc.)
  5. Gérer les animations et transitions
  6. Gérer les listeners
  7. Formater l'affichage
  8. Valider les actions
  9. Communiquer avec le serveur
  10. Logger les événements

Impact:
  - Impossible à tester (70+ champs à mocker)
  - Maintenance cauchemardesque
  - Bugs difficiles à tracer
  - Réutilisation impossible

Solution - Décomposer en 5 composants:

  1. GameStateViewModel
     - Gère uniquement l'état (lastState, shoppingView, etc.)
     - Observable pattern pour notifier les changements

  2. GameUIBuilder
     - Construction de l'interface
     - Panels, buttons, layouts

  3. KeyboardBindingManager
     - Gestion des raccourcis clavier
     - Registration/unregistration

  4. GameDialogManager (déjà partiellement fait)
     - Quiz dialogs
     - Exchange dialogs
     - Confirmation dialogs

  5. GameInteractionPresenter
     - Orchestration
     - 200-300 lignes maximum
     - Coordination entre les 4 autres


┌──────────────────────────────────────────────────────────────────────────┐
│ ÉLEVÉ #2: DUPLICATION MASSIVE toStringArray()                           │
└──────────────────────────────────────────────────────────────────────────┘

3 COPIES IDENTIQUES dans le backend:

1. panier-express.service.ts (lignes 994-1013):
  private toStringArray(value: unknown): string[] {
    if (Array.isArray(value)) {
      return value.map((v) => (v == null ? '' : String(v)))
                  .filter((v) => v.length > 0);
    }
    if (typeof value === 'string') {
      try {
        const parsed = JSON.parse(value);
        if (Array.isArray(parsed)) {
          return parsed.map((v) => (v == null ? '' : String(v)))
                      .filter((v) => v.length > 0);
        }
      } catch { /* ignore */ }
      return value.split(/[,;]+/).map((v) => v.trim())
                  .filter((v) => v.length > 0);
    }
    return [];
  }

2. panier-express-utils.service.ts (lignes 43-69):
  EXACTEMENT IDENTIQUE

3. panier-express-draw.service.ts (implicite):
  Variante similaire

Impact:
  - Maintenance ×3
  - Bugs ×3
  - Risque d'incohérences

Solution:
  Centraliser dans un utilitaire:

  // backend/src/common/utils/array-converter.util.ts
  export class ArrayConverter {
    static toStringArray(value: unknown): string[] {
      if (Array.isArray(value)) {
        return value.map((v) => (v == null ? '' : String(v)))
                    .filter((v) => v.length > 0);
      }
      if (typeof value === 'string') {
        try {
          const parsed = JSON.parse(value);
          if (Array.isArray(parsed)) {
            return parsed.map((v) => (v == null ? '' : String(v)))
                        .filter((v) => v.length > 0);
          }
        } catch { /* ignore */ }
        return value.split(/[,;]+/).map((v) => v.trim())
                    .filter((v) => v.length > 0);
      }
      return [];
    }
  }

  Puis remplacer partout par:
    ArrayConverter.toStringArray(value)


┌──────────────────────────────────────────────────────────────────────────┐
│ ÉLEVÉ #3: DUPLICATION MASSIVE standLabel()                              │
└──────────────────────────────────────────────────────────────────────────┘

3 COPIES IDENTIQUES:

1. panier-express.service.ts (lignes 624-636):
  private standLabel(standId: string | undefined): string {
    const raw = (standId ?? 'inconnu').trim();
    if (!raw) return 'inconnu';
    const tokenMap: Record<string, string> = {
      legumes: 'légumes',
      ete: 'été',
      maraicher: 'maraîcher',
    };
    return raw.split('-')
              .map((token) => tokenMap[token] ?? token)
              .join('-');
  }

2. panier-express-draw.service.ts (lignes 113-125):
  private formatStandId(standId: string | undefined): string {
    EXACTEMENT IDENTIQUE

3. panier-express-utils.service.ts (lignes 114-139):
  Variante getTileLabel()

Solution:
  // backend/src/game/games/.../panier-express/utils/stand-formatter.ts
  export class StandFormatter {
    private static readonly TOKEN_MAP: Record<string, string> = {
      legumes: 'légumes',
      ete: 'été',
      maraicher: 'maraîcher',
    };

    static formatStandId(standId: string | undefined): string {
      const raw = (standId ?? 'inconnu').trim();
      if (!raw) return 'inconnu';
      return raw.split('-')
                .map((token) => this.TOKEN_MAP[token] ?? token)
                .join('-');
    }
  }


┌──────────────────────────────────────────────────────────────────────────┐
│ ÉLEVÉ #4: DUPLICATION normalizePlayers()                                │
└──────────────────────────────────────────────────────────────────────────┘

2 COPIES:

1. panier-express.service.ts (lignes 454-458):
  private ensureMetadata(state: GameStateEntity): GameStateEntity {
    const normalizedPlayers = this.utils.normalizePlayers(
      state.players as PlayerStateEntity[] | undefined
    );
    // ...
  }

2. panier-express-utils.service.ts (lignes 38-41):
  normalizePlayers(players: any[] | undefined): PanierExpressPlayer[] {
    if (!Array.isArray(players)) return [];
    return players.map((p) => this.normalizePlayer(p));
  }

Aussi logique de normalisation dupliquée dans:
  panier-express.service.ts (lignes 254-257)

Solution:
  Utiliser UNIQUEMENT panier-express-utils.service.ts pour toute
  normalisation de joueurs.


┌──────────────────────────────────────────────────────────────────────────┐
│ ÉLEVÉ #5: REFACTORING INCOMPLET - game-ws                               │
└──────────────────────────────────────────────────────────────────────────┘

Modifications git récentes:
  D backend/src/game/ws/game-ws.handler.ts
  D backend/src/game/ws/game-ws.registrar.ts
  M backend/src/game/ws/game-ws.module.ts
  M backend/src/game/ws/dto/game-rules.dto.ts
  ?? backend/src/game/ws/service/

Fichiers déplacés:
  game-ws.handler.ts     → service/game-ws.handler.ts
  game-ws.registrar.ts   → service/game-ws.registrar.ts

Problème: REFACTORING SUPERFICIEL

GameWsHandler a toujours 11 dépendances:
  @Injectable()
  export class GameWsHandler {
    constructor(
      private readonly content: GameContentService,       // ✓ UTILISÉ
      private readonly board: BoardService,               // ✗ JAMAIS UTILISÉ
      private readonly cards: CardsService,               // ✗ JAMAIS UTILISÉ
      private readonly movement: MovementService,         // ✗ JAMAIS UTILISÉ
      private readonly inventory: InventoryService,       // ✗ JAMAIS UTILISÉ
      private readonly exchange: ExchangeService,         // ✗ JAMAIS UTILISÉ
      private readonly turn: TurnService,                 // ✗ JAMAIS UTILISÉ
      private readonly effects: EffectsService,           // ✗ JAMAIS UTILISÉ
      private readonly quiz: QuizService,                 // ✗ JAMAIS UTILISÉ
      private readonly victory: VictoryService,           // ✗ JAMAIS UTILISÉ
      private readonly validator: PayloadValidationService, // ✓ UTILISÉ
    ) {}

    async rules(session: WsSession, payload: any) {
      requireUser(session);
      const dto = this.validator.validate(GameRulesDto, payload);
      const gameType = dto.gameType;
      const rules = await this.content.getRules(gameType);  // ← content
      return { type: 'game.rules', payload: { rules, gameType } };
    }

    async modules(session: WsSession) {
      requireUser(session);
      const modules = [
        this.board.getOverview(),      // ← Appels aux 9 services
        this.cards.getOverview(),
        this.movement.getOverview(),
        this.inventory.getOverview(),
        this.exchange.getOverview(),
        this.turn.getOverview(),
        this.effects.getOverview(),
        this.quiz.getOverview(),
        this.victory.getOverview(),
      ];
      return { type: 'game.modules', payload: { modules } };
    }
  }

Impact:
  - modules() n'est peut-être JAMAIS appelé (à vérifier)
  - 9 services injectés pour rien
  - Violation du principe de dépendance minimale
  - Couplage inutile

Solution:

  Option 1 - Si modules() n'est pas utilisé:
    Supprimer la méthode et les 9 dépendances

  Option 2 - Si modules() est utilisé:
    Créer une abstraction:

    interface GameModuleProvider {
      getModules(): GameModuleOverview[];
    }

    @Injectable()
    export class GameModuleRegistry implements GameModuleProvider {
      private modules: GameModuleOverview[] = [];

      register(module: GameModuleOverview) {
        this.modules.push(module);
      }

      getModules(): GameModuleOverview[] {
        return this.modules;
      }
    }

    Puis:
    @Injectable()
    export class GameWsHandler {
      constructor(
        private readonly content: GameContentService,
        private readonly validator: PayloadValidationService,
        private readonly moduleRegistry: GameModuleRegistry,  // ← 1 au lieu de 9
      ) {}

      async modules(session: WsSession) {
        requireUser(session);
        const modules = this.moduleRegistry.getModules();
        return { type: 'game.modules', payload: { modules } };
      }
    }


┌──────────────────────────────────────────────────────────────────────────┐
│ ÉLEVÉ #6: TYPES DANGEREUX - unknown[]                                   │
└──────────────────────────────────────────────────────────────────────────┘

Fichier: backend/src/game/core/entities/game-state.entity.ts

Code problématique:
  export type PlayerStateEntity = {
    id: number;
    username: string;
    isBot: boolean;
    basket: unknown[];        // ← PERTE TOTALE de type
    inventory: unknown[];     // ← PERTE TOTALE de type
    shoppingList: unknown[];  // ← PERTE TOTALE de type
  };

Impact:
  - Aucune validation TypeScript
  - Bugs détectés seulement en runtime
  - Impossible de détecter les erreurs au compile-time
  - Nécessite des conversions constantes

Exemple de problème:
  const basket = player.basket;  // unknown[]
  basket.forEach(item => {
    // TypeScript ne sait pas que item est une string
    const name = item.toUpperCase();  // ← Erreur compile-time
  });

  // Il faut caster:
  basket.forEach(item => {
    const name = String(item).toUpperCase();  // ← Risqué
  });

Solution - Créer des types spécifiques:

  // Option 1: Type simple
  export type ShoppingItem = string;

  export type PlayerStateEntity = {
    id: number;
    username: string;
    isBot: boolean;
    basket: ShoppingItem[];
    inventory: ShoppingItem[];
    shoppingList: ShoppingItem[];
  };

  // Option 2: Branded types (meilleur)
  type Brand<K, T> = K & { __brand: T };
  type ShoppingItem = Brand<string, 'ShoppingItem'>;
  type InventoryItem = Brand<string, 'InventoryItem'>;

  export type PlayerStateEntity = {
    id: number;
    username: string;
    isBot: boolean;
    basket: ShoppingItem[];
    inventory: InventoryItem[];
    shoppingList: ShoppingItem[];
  };

  // Helper pour créer un ShoppingItem
  function createShoppingItem(value: string): ShoppingItem {
    return value as ShoppingItem;
  }


┌──────────────────────────────────────────────────────────────────────────┐
│ ÉLEVÉ #7: VALIDATION ABSENTE - DTOs                                     │
└──────────────────────────────────────────────────────────────────────────┘

Fichier: backend/src/game/ws/dto/game-rules.dto.ts

Code actuel:
  export class GameRulesDto {
    @IsString()
    @MinLength(1)
    gameType!: string;
  }

Problèmes:
  1. Pas de @MaxLength → DoS possible (string géante)
  2. Pas de @Matches → Injection possible
  3. Pas de whitelist → Types de jeu invalides acceptés

Impact:
  - Attaque DoS: envoyer "a".repeat(1000000)
  - Injection: envoyer "../../../etc/passwd"
  - Erreurs: envoyer un type de jeu inexistant

Solution:
  import { IsString, MinLength, MaxLength, Matches, IsIn } from
         'class-validator';

  export class GameRulesDto {
    @IsString()
    @MinLength(1)
    @MaxLength(100)  // ← Limite raisonnable
    @Matches(/^[a-z0-9-]+$/)  // ← Pattern sécurisé
    @IsIn(['panier-express', 'dame-nature', 'loup-garou'])  // ← Whitelist
    gameType!: string;
  }

Appliquer aussi à game-action.dto.ts:
  export class GameSingleActionDto {
    @IsString()
    @MaxLength(50)
    @Matches(/^[a-z_]+$/)
    type!: string;

    @IsObject()
    @ValidateNested()
    payload?: Record<string, any>;
  }


┌──────────────────────────────────────────────────────────────────────────┐
│ ÉLEVÉ #8: Threading issues - Client                                     │
└──────────────────────────────────────────────────────────────────────────┘

Fichier: GenericGameInteractionComponent.java

Variables non synchronisées:
  private Integer localPlayerId;  // Peut être modifié depuis EDT et listeners
  private GameQuizHandler quizHandler;
  private GameDialogManager dialogManager;

Scenario de race condition:
  Thread 1 (Realtime): quizHandler = new GameQuizHandler(...)
  Thread 2 (EDT):      if (quizHandler.isActive())  // ← NPE si null

Solution:
  Option 1: Rendre volatile
    private volatile GameQuizHandler quizHandler;

  Option 2: Synchroniser l'accès
    private final Object quizHandlerLock = new Object();
    private GameQuizHandler quizHandler;

    private void setQuizHandler(GameQuizHandler handler) {
      synchronized (quizHandlerLock) {
        this.quizHandler = handler;
      }
    }

  Option 3: Atomic reference
    private final AtomicReference<GameQuizHandler> quizHandler =
      new AtomicReference<>();


================================================================================
4. PROBLÈMES MOYENS
================================================================================

┌──────────────────────────────────────────────────────────────────────────┐
│ MOYEN #1: Erreurs silencieuses - Backend                                │
└──────────────────────────────────────────────────────────────────────────┘

Fichier: backend/src/game/engine/services/game-registry.service.ts

Code problématique (lignes 101-127):
  private async readDefinition(manifestPath: string, root: string):
      Promise<GameDefinition | null> {
    try {
      const raw = await fs.promises.readFile(manifestPath, 'utf-8');
      const data = JSON.parse(raw) as Record<string, any>;
      // ... traitement ...
    } catch (error) {
      this.logger.warn(`Manifest invalide ${manifestPath}: ${error}`);
      return null;  // ← Type d'erreur inconnu
    }
  }

Impact:
  Impossible de savoir si:
  - Le fichier n'existe pas
  - Le JSON est invalide
  - L'accès est refusé
  - Autre erreur

Solution:
  private async readDefinition(manifestPath: string, root: string):
      Promise<GameDefinition | null> {
    try {
      const raw = await fs.promises.readFile(manifestPath, 'utf-8');
      const data = JSON.parse(raw) as Record<string, any>;
      // ... traitement ...
    } catch (error) {
      if (error.code === 'ENOENT') {
        this.logger.debug(`Manifest non trouvé: ${manifestPath}`);
      } else if (error instanceof SyntaxError) {
        this.logger.error(`JSON invalide dans ${manifestPath}: ${error}`);
      } else {
        this.logger.error(`Erreur lecture manifest ${manifestPath}:`, error);
      }
      return null;
    }
  }


┌──────────────────────────────────────────────────────────────────────────┐
│ MOYEN #2: Erreurs silencieuses - Client                                 │
└──────────────────────────────────────────────────────────────────────────┘

Fichier: GenericGameInteractionComponent.java

Code problématique (lignes 763-779):
  private Integer decodeUserIdFromToken(String token) {
    if (token == null || token.isBlank()) return null;
    try {
      String[] parts = token.split("\\.");
      if (parts.length < 2) return null;
      String payload = new String(Base64.getUrlDecoder().decode(parts[1]));
      JsonNode node = mapper.readTree(payload);
      if (node.has("id") && node.get("id").isInt()) {
        return node.get("id").asInt();
      }
      if (node.has("userId") && node.get("userId").isInt()) {
        return node.get("userId").asInt();
      }
    } catch (Exception ex) {
      LOGGER.debug("[token] decode failed: {}", ex.getMessage());  // ← DEBUG
    }
    return null;
  }

Problème:
  - Exception catchée avec log DEBUG seulement
  - Impossible de déboguer en production
  - Aucune indication du type d'erreur

Solution:
  } catch (IllegalArgumentException ex) {
    LOGGER.warn("[token] Format invalide: {}", ex.getMessage());
  } catch (JsonProcessingException ex) {
    LOGGER.warn("[token] JSON invalide: {}", ex.getMessage());
  } catch (Exception ex) {
    LOGGER.error("[token] Erreur inattendue", ex);  // ← Stack trace
  }


┌──────────────────────────────────────────────────────────────────────────┐
│ MOYEN #3: Perte d'actions - Client                                      │
└──────────────────────────────────────────────────────────────────────────┘

Fichier: AbstractRealtimeService.java

Code problématique:
  private final Queue<Consumer<C>> pending = new ConcurrentLinkedQueue<>();
  private static final int MAX_PENDING_ACTIONS = 200;

  private void enqueue(Consumer<C> task) {
    if (pending.size() >= MAX_PENDING_ACTIONS) {
      pending.poll();  // ← SUPPRESSION SILENCIEUSE
    }
    pending.add(task);
  }

Impact:
  - Les actions les plus anciennes sont supprimées sans warning
  - Joueur peut perdre des actions critiques
  - Aucun feedback utilisateur

Solution:
  private void enqueue(Consumer<C> task) {
    if (pending.size() >= MAX_PENDING_ACTIONS) {
      Consumer<C> dropped = pending.poll();
      LOGGER.warn("Queue pleine, action abandonnée: {}", dropped);
      // Optionnel: notifier l'utilisateur
      eventBus.publish(new ActionDroppedEvent(dropped));
    }
    pending.add(task);
  }


┌──────────────────────────────────────────────────────────────────────────┐
│ MOYEN #4: Conversion hasardeuse - Client                                │
└──────────────────────────────────────────────────────────────────────────┘

Fichier: GenericGameInteractionComponent.java (lignes 581-600)

Code problématique:
  private Map<String, Object> toPayload(Object payload) {
    if (payload == null) return Map.of();
    if (payload instanceof Map<?, ?> map) {
      Map<String, Object> safe = new LinkedHashMap<>();
      map.forEach((k, v) -> {
        if (k != null) {
          safe.put(k.toString(), v);
        }
      });
      return safe;
    }
    if (payload instanceof JsonNode node) {
      return mapper.convertValue(node, Map.class);
    }
    try {
      return mapper.convertValue(payload, Map.class);
    } catch (IllegalArgumentException ex) {
      return Collections.emptyMap();  // ← BUG SILENCIEUX
    }
  }

Impact:
  - Si convertValue échoue, Map vide retournée
  - Action envoyée SANS payload au serveur
  - Erreur côté serveur possible

Solution:
  try {
    return mapper.convertValue(payload, Map.class);
  } catch (IllegalArgumentException ex) {
    LOGGER.error("Impossible de convertir payload: {}", payload, ex);
    throw new IllegalStateException("Payload invalide", ex);  // ← Fail fast
  }


┌──────────────────────────────────────────────────────────────────────────┐
│ MOYEN #5: Type casting dangereux - Backend                              │
└──────────────────────────────────────────────────────────────────────────┘

Exemples multiples:

1. panier-express-exchange.service.ts (ligne 81):
  const meta = state.metadata as PanierExpressMetadata;  // ← Cast sans vérif

2. panier-express.service.ts (ligne 455):
  const normalizedPlayers = this.utils.normalizePlayers(
    state.players as PlayerStateEntity[] | undefined
  );

3. panier-express.service.ts (ligne 220):
  const hasList = Array.isArray(p.shoppingList) && p.shoppingList.length > 0;
  // p.shoppingList est unknown[], pas de validation de contenu

Solution:
  Créer des type guards:

  function isPanierExpressMetadata(value: any):
      value is PanierExpressMetadata {
    return value &&
           typeof value === 'object' &&
           'positions' in value &&
           'tiles' in value &&
           'decks' in value;
  }

  // Utilisation:
  if (!isPanierExpressMetadata(state.metadata)) {
    throw new Error('Metadata invalide');
  }
  const meta = state.metadata;  // Type sûr


┌──────────────────────────────────────────────────────────────────────────┐
│ MOYEN #6: Duplication - Boucles client                                  │
└──────────────────────────────────────────────────────────────────────────┘

Fichier: GenericGameInteractionComponent.java

3 patterns similaires:

1. handleActionShortcut (ligne ~830):
  for (int i = 0; i < actionsModel.size(); i++) {
    GenericGameState.GenericAction act = actionsModel.get(i);
    if (act != null && targetType.equalsIgnoreCase(act.type())) {
      // ...
    }
  }

2. dispatchActionMatching (ligne ~850):
  for (int i = 0; i < actionsModel.size(); i++) {
    GenericGameState.GenericAction act = actionsModel.get(i);
    if (act == null) continue;
    if (matcher.test(act)) {
      // ...
    }
  }

3. Autres variantes similaires

Solution:
  Créer une classe helper ActionListModel:

  public class ActionListModel {
    private final DefaultListModel<GenericAction> model;

    public Optional<GenericAction> findByType(String type) {
      return IntStream.range(0, model.size())
        .mapToObj(model::get)
        .filter(Objects::nonNull)
        .filter(a -> type.equalsIgnoreCase(a.type()))
        .findFirst();
    }

    public Optional<GenericAction> findMatching(Predicate<GenericAction> pred) {
      return IntStream.range(0, model.size())
        .mapToObj(model::get)
        .filter(Objects::nonNull)
        .filter(pred)
        .findFirst();
    }
  }


┌──────────────────────────────────────────────────────────────────────────┐
│ MOYEN #7: Commentaire garbled - Client                                  │
└──────────────────────────────────────────────────────────────────────────┘

Fichier: GenericGameInteractionComponent.java (ligne 879)

Code:
  private void handleDrawShortcut() {
    // ...
    if (!tableState.started()) {
      return; // Pas de pioche avant le dンmarrage de la partie.
      //                                 ^^^ UTF-8 garbled
    }
  }

Problème: Encodage UTF-8 mal géré (マ → ン)

Solution:
  Vérifier l'encodage du fichier (doit être UTF-8)


================================================================================
5. ANALYSE BACKEND DÉTAILLÉE
================================================================================

ARCHITECTURE GÉNÉRALE
---------------------

Structure:
  backend/src/
  ├── game/                          # Module principal du jeu
  │   ├── engine/                    # Moteur de jeu générique
  │   │   ├── gateways/
  │   │   │   └── game.gateway.ts   # WebSocket gateway
  │   │   ├── services/
  │   │   │   ├── game-engine.service.ts
  │   │   │   ├── game-registry.service.ts
  │   │   │   ├── game-engine-state.store.ts
  │   │   │   └── game-content.service.ts
  │   │   └── interfaces/
  │   │       └── game-rules-adapter.interface.ts
  │   ├── ws/                        # WebSocket routing
  │   │   ├── game-ws.module.ts
  │   │   ├── dto/
  │   │   │   └── game-rules.dto.ts
  │   │   └── service/               # NOUVEAU (refactoring)
  │   │       ├── game-ws.handler.ts
  │   │       └── game-ws.registrar.ts
  │   ├── games/                     # Jeux spécialisés
  │   │   ├── jeux-de-plateaux/
  │   │   │   └── les-quatre-vents/
  │   │   │       └── panier-express/
  │   │   │           ├── services/
  │   │   │           │   ├── panier-express.service.ts (1040 lignes)
  │   │   │           │   ├── panier-express-setup.service.ts
  │   │   │           │   ├── panier-express-draw.service.ts
  │   │   │           │   ├── panier-express-exchange.service.ts
  │   │   │           │   └── panier-express-quiz.service.ts
  │   │   │           ├── entities/
  │   │   │           ├── definitions/
  │   │   │           └── utils/
  │   │   └── jeux-de-cartes/
  │   └── modules/                   # Services génériques réutilisables
  │       ├── turn/
  │       ├── board/
  │       ├── cards/
  │       ├── effects/
  │       ├── quiz/
  │       ├── exchange/
  │       ├── inventory/
  │       ├── movement/
  │       ├── victory/
  │       ├── bot/
  │       └── actionlog/
  └── common/
      ├── ws/
      ├── utils/
      └── validation/

PATTERNS UTILISÉS
-----------------

✓ Dependency Injection (DI): Utilisation cohérente de @Injectable()
✓ Module pattern: Architecture modulaire claire avec @Module()
✓ Gateway pattern: game.gateway.ts pour WebSocket
✓ Service Locator (minimal): GameRulesAdapter registré dynamiquement
✓ State store: GameEngineStateStore pour l'état en mémoire

✗ Repository pattern: Non utilisé (stockage en mémoire seulement)
✗ CQRS: Non implémenté
✗ Event Sourcing: Non implémenté

WEBSOCKET - game.gateway.ts
----------------------------

Code principal (lignes 14-261):
  @WebSocketGateway({ path: '/ws/game' })
  export class GameGateway implements OnGatewayConnection, OnGatewayDisconnect {
    @WebSocketServer()
    server!: Server<WebSocket>;

    private readonly clients = new Map<WebSocket, GameClient>();
    private readonly rooms = new Map<string, Set<WebSocket>>();
    private readonly heartbeats = new Map<WebSocket, NodeJS.Timeout>();
    private readonly lastPong = new WeakMap<WebSocket, number>();

Points positifs:
  ✓ Heartbeat correctement implémenté (ping/pong)
  ✓ Gestion de la déconnexion propre
  ✓ Broadcasting efficace par room
  ✓ Authentification JWT

Points négatifs:
  ✗ Pas de rate limiting
  ✗ Pas de validation stricte des messages
  ✗ Pas de logs d'audit détaillés

Messages supportés:
  - game.join: Rejoindre une partie
  - game.state: Demander l'état
  - game.actions: Envoyer des actions
  - game.bot.play: Forcer le bot à jouer

GAME ENGINE - game-engine.service.ts
-------------------------------------

Responsabilités:
  1. Gérer les états de jeu
  2. Appliquer les actions
  3. Vérifier les accès (checkAccess)
  4. Broadcaster les états
  5. Gérer les tours de bot

Code important (lignes 68-104):
  async applyActions(
    roomId: number,
    gameType: string,
    actions: GameSingleActionDto[],
    actorId: number | null,
    allowBotTurn = false,
  ): Promise<GameStateResponse> {
    const current = await this.getState(roomId, gameType);

    // Validation basique
    const currentPlayerId = current.turn?.currentPlayerId ?? null;
    if (currentPlayerId !== actorId) {
      throw new UnauthorizedException("Ce n'est pas votre tour.");
    }

    // Application des actions
    const sanitizedActions = Array.isArray(actions)
      ? actions.map((action) => ({
          ...action,
          meta: { ...(action?.meta ?? {}), actor: actorLabel },
        }))
      : [];

    const next = await handler.applyActions(current, sanitizedActions);
    await this.store.set(roomId, gameType, next);
    this.broadcaster?.(gameType, roomId, this.toResponse(next));
    return this.toResponse(next);
  }

Problèmes:
  ✗ Validation minimale des actions
  ✗ Pas de validation du payload
  ✗ Pas de limite sur le nombre d'actions

PANIER EXPRESS - Analyse détaillée
-----------------------------------

Service principal: panier-express.service.ts (1040 lignes)

Complexité cyclomatic: TRÈS ÉLEVÉE

Méthodes principales:
  - exposeState (139 lignes): Construit la vue pour le client
  - hydrateInitialState (74 lignes): Initialise une nouvelle partie
  - applyActions (11 lignes): Orchestre l'application d'actions
  - dispatchAction (18 lignes): Route vers le bon handler
  - getBotActions (54 lignes): IA du bot
  - getAvailableActions (37 lignes): Actions disponibles pour joueur
  - handleRoll (32 lignes): Gérer lancer de dé
  - handleAnswerQuiz (38 lignes): Gérer réponse quiz
  - movePlayer (18 lignes): Déplacer un joueur
  - resolveTile (13 lignes): Résoudre effet d'une tuile
  - advanceTurn (32 lignes): Passer au tour suivant
  - applyVictory (16 lignes): Vérifier conditions de victoire

État fragmenté dans:
  1. state.metadata.positions (positions joueurs)
  2. state.metadata.quiz.pending (quiz en attente)
  3. state.metadata.statuses.skipTurn (tours sautés)
  4. state.pending (échange en attente)
  5. state.players[].basket
  6. state.players[].inventory
  7. state.players[].shoppingList

Logique de roll complexe (lignes 545-577):
  1. Valider joueur actuel
  2. Générer/valider le dé
  3. Logger l'action
  4. Déplacer le joueur
  5. Résoudre la tuile
  6. Vérifier si quiz/échange bloque
  7. Avancer le tour si pas bloqué

MODULES GÉNÉRIQUES
------------------

Liste des modules:
  - TurnService: Gestion des tours
  - BoardService: Plateau de jeu
  - CardsService: Cartes
  - MovementService: Déplacements
  - InventoryService: Inventaire
  - ExchangeService: Échanges
  - EffectsService: Effets spéciaux
  - QuizService: Questions/réponses
  - VictoryService: Conditions de victoire
  - BotRunnerService: IA des bots
  - ActionLogService: Historique des actions

Points positifs:
  ✓ Bonne séparation des responsabilités au niveau modules
  ✓ Réutilisables entre différents jeux
  ✓ Testables indépendamment

Points négatifs:
  ✗ Certains modules trop génériques (peu utilisés)
  ✗ Interfaces pas toujours bien définies


================================================================================
6. ANALYSE CLIENT JAVA DÉTAILLÉE
================================================================================

ARCHITECTURE GÉNÉRALE
---------------------

Structure:
  com.lemondelila.client
  ├── admin/               (gestion admin)
  ├── application/         (branding, i18n, écrans)
  ├── chat/                (chat temps réel)
  ├── game/
  │   ├── bot/             (gestion des bots)
  │   ├── catalog/         (catalogue de jeux)
  │   ├── core/            (composants génériques)
  │   │   ├── controller/  (GenericGameInteractionController)
  │   │   ├── model/       (GenericGameState)
  │   │   ├── view/        (GenericGameInteractionComponent + renderers)
  │   │   ├── service/     (services métier)
  │   │   └── viewmodel/   (formatters, calculateurs)
  │   ├── history/         (historique parties)
  │   ├── quiz/            (système quiz)
  │   ├── realtime/        (gestion temps réel WSS)
  │   ├── room/            (gestion salles)
  │   └── session/         (session joueur)
  ├── framework/
  │   ├── network/
  │   │   └── realtime/    (AbstractRealtimeService)
  │   └── ui/
  ├── home/                (écran accueil)
  ├── menu/                (menu principal)
  ├── messaging/           (messagerie privée)
  ├── presence/            (liste connectés)
  ├── security/            (chiffrement, auth)
  ├── settings/            (options, MAJ)
  └── user/                (login, registration)

PATTERNS UTILISÉS
-----------------

✓ MVP (Model-View-Presenter):
  - GenericGameInteractionComponent = View (Swing JPanel)
  - GenericGameInteractionController = Controller/Presenter
  - GenericGameState = Model

✓ Observer Pattern:
  GenericGameInteractionController.java:
    private final List<Consumer<GenericGameState>> stateObservers =
        new CopyOnWriteArrayList<>();
    private volatile Listener listener;

✓ Renderer Pattern (séparation logique affichage):
  - GameStatusRenderer
  - GameActionsRenderer
  - GameTurnRenderer
  - GamePendingRenderer
  - GameExchangeRenderer
  - GamePlayerCollectionsRenderer
  - GameQuizHandler
  - GameDialogManager

✓ Factory Pattern:
  - GameQuizComponentFactory pour créer composants quiz

✓ Realtime Service Pattern:
  - AbstractRealtimeService<K, C> base générique
  - Reconnexion avec backoff exponentiel

WEBSOCKET/SSE - AbstractRealtimeService
----------------------------------------

Code excellent (lignes 39-51):
  private final Object lock = new Object();
  private final Queue<Consumer<C>> pending = new ConcurrentLinkedQueue<>();
  private volatile int activeClients;
  private volatile boolean closed;
  private volatile boolean connected;
  private volatile K desiredKey;
  private volatile K connectionKey;
  private volatile C connection;

Reconnexion avec backoff (lignes ~180):
  private long computeDelayMillis(int attempt) {
    int exponent = Math.max(0, attempt - 1);
    long base = INITIAL_RETRY_DELAY.toMillis() * (1L << exponent);
    base = Math.min(base, MAX_RETRY_DELAY.toMillis());
    long jitter = 250L + (long) (Math.random() * 1000L);  // ✓ Jitter
    return base + jitter;
  }

Points positifs:
  ✓ Excellent pattern de reconnexion
  ✓ Thread-safety correcte
  ✓ Gestion propre du lifecycle

Points négatifs:
  ✗ Queue limitée avec suppression silencieuse
  ✗ Pas de métrique sur les reconnexions

GENERICGAMEINTERACTIONCOMPONENT
--------------------------------

Statistiques:
  - Lignes: 1032
  - Champs privés: 70+
  - Méthodes privées: 30+
  - Méthodes publiques: 10+

Champs (échantillon):
  private GenericGameState lastState;
  private List<String> lastSelfShopping = List.of();
  private List<String> lastSelfBasket = List.of();
  private List<String> lastSelfInventory = List.of();
  private List<String> shoppingView = List.of();
  private List<String> basketView = List.of();
  private List<String> inventoryView = List.of();
  private List<Map<String, Object>> dynamicShortcuts = List.of();
  private Integer lastRollSeen;
  private boolean exchangePending;
  private boolean gameStarted;
  private boolean startAnnounced;
  private boolean firstStateRendered;
  private boolean autoPrimaryDispatched;
  private volatile Integer localPlayerId;
  private GameQuizHandler quizHandler;
  private GameDialogManager dialogManager;
  private GameActionEmitter emitter;
  private GameAnnouncementService announcementService;
  private TurnAnnouncementTracker turnAnnouncementTracker;
  private GameStatusRenderer statusRenderer;
  private GameActionsRenderer actionsRenderer;
  private GameTurnRenderer turnRenderer;
  private GamePendingRenderer pendingRenderer;
  private GameExchangeRenderer exchangeRenderer;
  private GamePlayerCollectionsRenderer collectionsRenderer;
  // ... 50+ autres

Responsabilités (trop nombreuses):
  1. Gérer l'état du jeu
  2. Construire l'UI
  3. Gérer les raccourcis clavier (70+ bindings)
  4. Gérer les dialogs
  5. Gérer les animations
  6. Gérer les listeners
  7. Formater l'affichage
  8. Valider les actions
  9. Communiquer avec le serveur
  10. Logger les événements

Méthodes importantes:
  - onState(GenericGameState state): Mise à jour de l'état
  - renderState(GenericGameState state): Rendu de l'interface
  - handleDrawShortcut(): Raccourci pioche
  - handleInterfaceShortcut(String id): Raccourcis interface
  - handleActionShortcut(String targetType): Raccourcis actions
  - dispatchPrimaryAction(): Envoyer action principale

RENDERERS
---------

GameStatusRenderer:
  - Affiche statut de la partie
  - Gère les logs de jeu
  - Scrolling automatique

GameActionsRenderer:
  - Affiche les actions disponibles
  - JList avec sélection
  - Listeners sur selection

GameTurnRenderer:
  - Affiche le tour actuel
  - "C'est à X de jouer"
  - Indicateur visuel

GamePendingRenderer:
  - Affiche actions en attente
  - Quiz, échanges, etc.

GameExchangeRenderer:
  - Interface d'échange
  - Sélection "donner" / "recevoir"
  - Validation

INTERNATIONALISATION
---------------------

messages.properties: 540+ entrées

Exemples:
  game.turn.player=C'est à {0} de jouer
  game.game.started=La partie a commencé
  panier.shortcut.l.desc=Lancer le dé
  panier.shortcut.s.desc=Voir ma liste
  panier.shortcut.b.desc=Voir mon panier

Utilisation:
  Internationalization.text("game.game.started")
  Internationalization.text("game.turn.player", playerName)

Points positifs:
  ✓ Toutes les chaînes externalisées
  ✓ Noms de clés bien structurés
  ✓ Support multi-langue possible

SÉCURITÉ - EncryptedSessionVault
---------------------------------

Chiffrement: AES/GCM (correct):
  private static final String TRANSFORMATION = "AES/GCM/NoPadding";
  private static final int GCM_TAG_LENGTH = 128;
  private static final int IV_LENGTH = 12;

Stockage (problématique):
  public static EncryptedSessionVault defaultVault() {
    Path directory = Path.of("config");
    return new EncryptedSessionVault(
      directory.resolve("session.bin"),     // Données chiffrées
      directory.resolve(".session.key")     // ← CLÉ EN CLAIR
    );
  }

Token JWT décodé localement:
  private Integer decodeUserIdFromToken(String token) {
    String[] parts = token.split("\\.");
    String payload = new String(Base64.getUrlDecoder().decode(parts[1]));
    // Parse JSON
  }

Points positifs:
  ✓ Ne valide PAS la signature (laisse ça au serveur)
  ✓ Lit seulement les claims publics

Points négatifs:
  ✗ Clé de chiffrement sur disque


================================================================================
7. DUPLICATIONS DE CODE
================================================================================

BACKEND
-------

1. toStringArray() - 3 COPIES
   ├── panier-express.service.ts (lignes 994-1013)
   ├── panier-express-utils.service.ts (lignes 43-69)
   └── panier-express-draw.service.ts (implicite)

2. standLabel() / formatStandId() - 3 COPIES
   ├── panier-express.service.ts (lignes 624-636)
   ├── panier-express-draw.service.ts (lignes 113-125)
   └── panier-express-utils.service.ts (lignes 114-139)

3. normalizePlayers() - 2 COPIES
   ├── panier-express.service.ts (lignes 454-458)
   └── panier-express-utils.service.ts (lignes 38-41)

4. Validation Array.isArray() + map/filter - PATTERN RÉPÉTÉ
   - Partout dans panier-express.service.ts
   - panier-express-draw.service.ts
   - panier-express-exchange.service.ts

5. Gestion des positions - LOGIQUE DUPLIQUÉE
   - panier-express.service.ts (ensurePlayerPositions)
   - panier-express-utils.service.ts (possiblement)

CLIENT
------

1. Boucles sur actionsModel - 3 PATTERNS SIMILAIRES
   ├── handleActionShortcut (ligne ~830)
   ├── dispatchActionMatching (ligne ~850)
   └── Autres variantes

2. Validation null + cast - PATTERN RÉPÉTÉ
   - Partout dans GenericGameInteractionComponent
   - if (x != null) { String s = (String) x; }

3. SwingUtilities.invokeLater - PATTERN RÉPÉTÉ
   - 20+ occurrences
   - Parfois avec race conditions

4. Map.of() conversions - LOGIQUE SIMILAIRE
   - toPayload() dans GenericGameInteractionComponent
   - Autres conversions Map/JsonNode


================================================================================
8. REFACTORING game-ws (ANALYSE)
================================================================================

CHANGEMENTS EFFECTUÉS
---------------------

Git status:
  M backend/src/game/engine/gateways/game.gateway.ts
  M backend/src/game/games/jeux-de-plateaux/les-quatre-vents/
    panier-express/services/panier-express-draw.service.ts
  M backend/src/game/games/jeux-de-plateaux/les-quatre-vents/
    panier-express/services/panier-express.service.ts
  M backend/src/game/ws/dto/game-rules.dto.ts
  D backend/src/game/ws/game-ws.handler.ts
  D backend/src/game/ws/game-ws.registrar.ts
  M backend/src/game/ws/game-ws.module.ts
  ?? backend/src/game/ws/service/

Fichiers déplacés:
  backend/src/game/ws/game-ws.handler.ts
    → backend/src/game/ws/service/game-ws.handler.ts

  backend/src/game/ws/game-ws.registrar.ts
    → backend/src/game/ws/service/game-ws.registrar.ts

AVANT vs APRÈS
--------------

AVANT:
  backend/src/game/ws/
  ├── game-ws.handler.ts
  ├── game-ws.registrar.ts
  ├── dto/
  │   └── game-rules.dto.ts
  └── game-ws.module.ts

APRÈS:
  backend/src/game/ws/
  ├── service/
  │   ├── game-ws.handler.ts
  │   └── game-ws.registrar.ts
  ├── dto/
  │   └── game-rules.dto.ts
  └── game-ws.module.ts

PROBLÈMES NON RÉSOLUS
---------------------

1. GameWsHandler a toujours 11 dépendances

Code actuel (service/game-ws.handler.ts):
  @Injectable()
  export class GameWsHandler {
    constructor(
      private readonly content: GameContentService,       // ✓ UTILISÉ (rules)
      private readonly board: BoardService,               // ✗ JAMAIS UTILISÉ
      private readonly cards: CardsService,               // ✗ JAMAIS UTILISÉ
      private readonly movement: MovementService,         // ✗ JAMAIS UTILISÉ
      private readonly inventory: InventoryService,       // ✗ JAMAIS UTILISÉ
      private readonly exchange: ExchangeService,         // ✗ JAMAIS UTILISÉ
      private readonly turn: TurnService,                 // ✗ JAMAIS UTILISÉ
      private readonly effects: EffectsService,           // ✗ JAMAIS UTILISÉ
      private readonly quiz: QuizService,                 // ✗ JAMAIS UTILISÉ
      private readonly victory: VictoryService,           // ✗ JAMAIS UTILISÉ
      private readonly validator: PayloadValidationService, // ✓ UTILISÉ
    ) {}

2. Seulement 2 méthodes

  async rules(session: WsSession, payload: any) {
    requireUser(session);
    const dto = this.validator.validate(GameRulesDto, payload);
    const gameType = dto.gameType;
    const rules = await this.content.getRules(gameType);
    return { type: 'game.rules', payload: { rules, gameType } };
  }

  async modules(session: WsSession) {
    requireUser(session);
    const modules = [
      this.board.getOverview(),
      this.cards.getOverview(),
      this.movement.getOverview(),
      this.inventory.getOverview(),
      this.exchange.getOverview(),
      this.turn.getOverview(),
      this.effects.getOverview(),
      this.quiz.getOverview(),
      this.victory.getOverview(),
    ];
    return { type: 'game.modules', payload: { modules } };
  }

Question: modules() est-il vraiment utilisé ?

3. GameWsRegistrar minimal

Code actuel (service/game-ws.registrar.ts):
  @Injectable()
  export class GameWsRegistrar implements OnModuleInit {
    constructor(
      private readonly registry: WsRouteRegistry,
      private readonly handler: GameWsHandler,
    ) {}

    onModuleInit() {
      this.registry.register('game.rules',
        (session, payload) => this.handler.rules(session, payload));
      this.registry.register('game.modules',
        (session) => this.handler.modules(session));
    }
  }

Enregistre seulement 2 routes.

VERDICT
-------

Refactoring: SUPERFICIEL

Points positifs:
  ✓ Meilleure organisation (service/ subdirectory)
  ✓ Clarté accrue sur responsabilités
  ✓ Pas de régression fonctionnelle

Points négatifs:
  ✗ N'a PAS résolu les vrais problèmes
  ✗ 9 dépendances mortes toujours présentes
  ✗ Aucun changement architectural
  ✗ Pas d'amélioration de testabilité
  ✗ Pas d'optimisation

CE QUI AURAIT DÛ ÊTRE FAIT
---------------------------

1. Analyse d'utilisation
   - Vérifier si modules() est appelé
   - Tracer les routes WebSocket utilisées

2. Si modules() N'EST PAS utilisé:
   - Supprimer la méthode
   - Supprimer les 9 dépendances
   - Garder seulement content + validator

3. Si modules() EST utilisé:
   - Créer GameModuleRegistry
   - Injection d'une seule dépendance au lieu de 9
   - Pattern registry pour enregistrement dynamique

4. Tests unitaires
   - Ajouter des tests pour rules()
   - Ajouter des tests pour modules() si utilisé
   - Mocker seulement les dépendances nécessaires

5. Documentation
   - Documenter l'architecture
   - Expliquer le rôle de chaque service


================================================================================
9. RECOMMANDATIONS PRIORITAIRES
================================================================================

┌──────────────────────────────────────────────────────────────────────────┐
│ URGENT - Cette semaine                                                   │
└──────────────────────────────────────────────────────────────────────────┘

1. Corriger le leak SseLogger (CLIENT)
   Priorité: CRITIQUE
   Temps estimé: 2 heures

   Actions:
   a) Transformer SseLogger en singleton
   b) Ajouter synchronisation (synchronized ou Lock)
   c) Ajouter buffering pour performance
   d) Vérifier toutes les utilisations
   e) Tester avec charge élevée

   Code:
     public class SseLogger {
       private static final SseLogger INSTANCE = new SseLogger();
       private final Object lock = new Object();
       private final BufferedWriter writer;

       private SseLogger() {
         try {
           this.writer = Files.newBufferedWriter(
             Path.of("logsse.txt"),
             StandardOpenOption.CREATE,
             StandardOpenOption.APPEND
           );
         } catch (IOException e) {
           throw new UncheckedIOException(e);
         }
       }

       public static SseLogger getInstance() {
         return INSTANCE;
       }

       public void log(String text) {
         synchronized (lock) {
           try {
             writer.write(text);
             writer.newLine();
             writer.flush();
           } catch (IOException e) {
             System.err.println("Log error: " + e);
           }
         }
       }
     }

2. Corriger la race condition EDT (CLIENT)
   Priorité: CRITIQUE
   Temps estimé: 1 heure

   Actions:
   a) Déplacer lastState = state dans invokeLater
   b) Vérifier toutes les méthodes onXxx
   c) Ajouter assertions EDT (@RequiresEdt si disponible)
   d) Tester avec EDT violation checker

   Code:
     @Override
     public void onState(GenericGameState state) {
       SwingUtilities.invokeLater(() -> {
         this.lastState = state;  // ← DANS EDT
         renderState(state);
       });
     }

     @Override
     public void onError(String message) {
       SwingUtilities.invokeLater(() -> {
         emitter.announceError(message);
         statusRenderer.clear();
         turnAnnouncementTracker.clearLastSeen();
         statusRenderer.resetLogs();
       });
     }

3. Ajouter persistance backend (BACKEND)
   Priorité: CRITIQUE
   Temps estimé: 1-2 jours

   Actions:
   a) Choisir stratégie (TypeORM vs Redis)
   b) Créer entité/schema GameState
   c) Implémenter repository
   d) Ajouter sauvegarde auto (après chaque action)
   e) Ajouter chargement au démarrage
   f) Tester récupération après crash

   Option 1 - TypeORM (persistence durable):
     @Entity()
     export class GameStateEntity {
       @PrimaryColumn()
       id: string;  // roomId:gameType

       @Column('json')
       state: any;

       @Column()
       roomId: number;

       @Column()
       gameType: string;

       @CreateDateColumn()
       createdAt: Date;

       @UpdateDateColumn()
       updatedAt: Date;
     }

     @Injectable()
     export class GameStateRepository {
       constructor(
         @InjectRepository(GameStateEntity)
         private repo: Repository<GameStateEntity>
       ) {}

       async save(roomId: number, gameType: string, state: any) {
         const id = `${roomId}:${gameType}`;
         await this.repo.save({ id, roomId, gameType, state });
       }

       async load(roomId: number, gameType: string) {
         const entity = await this.repo.findOne({
           where: { id: `${roomId}:${gameType}` }
         });
         return entity?.state;
       }

       async deleteOlderThan(days: number) {
         const cutoff = new Date();
         cutoff.setDate(cutoff.getDate() - days);
         await this.repo.delete({
           updatedAt: LessThan(cutoff)
         });
       }
     }

   Option 2 - Redis (performance):
     @Injectable()
     export class GameStateCache {
       constructor(@Inject('REDIS') private redis: Redis) {}

       async save(roomId: number, gameType: string, state: any) {
         const key = `game:${roomId}:${gameType}`;
         const ttl = 7200; // 2 heures
         await this.redis.setex(key, ttl, JSON.stringify(state));
       }

       async load(roomId: number, gameType: string) {
         const key = `game:${roomId}:${gameType}`;
         const json = await this.redis.get(key);
         return json ? JSON.parse(json) : null;
       }
     }

4. Décomposer PanierExpressService (BACKEND)
   Priorité: CRITIQUE
   Temps estimé: 3-5 jours

   Phase 1 - Extraire le Presenter:
     // panier-express-state.presenter.ts
     @Injectable()
     export class PanierExpressStatePresenter {
       constructor(
         private readonly utils: PanierExpressUtils,
       ) {}

       exposeState(state: GameStateEntity): GameStateWithActions {
         // Déplacer toute la logique de buildView ici
       }

       private buildPlayerViews(state: GameStateEntity) { }
       private buildPendingView(...) { }
       private buildExtrasView(...) { }
     }

   Phase 2 - Extraire l'Orchestrator:
     // panier-express.orchestrator.ts
     @Injectable()
     export class PanierExpressOrchestrator {
       constructor(
         private readonly core: GameCoreService,
         private readonly resolver: ActionResolverService,
         private readonly actionHandler: PanierExpressActionHandler,
         private readonly turnManager: PanierExpressTurnManager,
         private readonly victoryChecker: PanierExpressVictoryChecker,
       ) {}

       applyActions(state: GameStateEntity, actions: GameSingleActionDto[]) {
         let next = this.ensureStarted(state);
         next = this.resolver.apply(next, actions,
           (s, a) => this.actionHandler.dispatch(s, a));
         next = this.victoryChecker.check(next);
         return next;
       }
     }

   Phase 3 - Extraire l'ActionHandler:
     // panier-express-action.handler.ts
     @Injectable()
     export class PanierExpressActionHandler {
       dispatch(state: GameStateEntity, action: GameSingleActionDto) {
         switch (action.type) {
           case 'roll': return this.handleRoll(state, action);
           case 'answer_quiz': return this.handleAnswerQuiz(state, action);
           case 'exchange_with': return this.handleExchangeWith(state, action);
           default: return state;
         }
       }

       private handleRoll(...) { }
       private handleAnswerQuiz(...) { }
       private handleExchangeWith(...) { }
     }

   Phase 4 - Extraire le BotStrategy:
     // panier-express-bot.strategy.ts
     @Injectable()
     export class PanierExpressBotStrategy {
       getBotActions(state: GameStateEntity, botPlayerId: number) {
         // Déplacer toute la logique de bot ici
       }
     }

   Phase 5 - Service principal devient coordinateur:
     @Injectable()
     export class PanierExpressService implements GameRulesAdapter {
       constructor(
         private readonly orchestrator: PanierExpressOrchestrator,
         private readonly presenter: PanierExpressStatePresenter,
         private readonly initializer: PanierExpressStateInitializer,
         private readonly botStrategy: PanierExpressBotStrategy,
       ) {}

       exposeState(state: GameStateEntity) {
         return this.presenter.exposeState(state);
       }

       hydrateInitialState(baseState: GameStateEntity) {
         return this.initializer.hydrate(baseState);
       }

       applyActions(state: GameStateEntity, actions: GameSingleActionDto[]) {
         return this.orchestrator.applyActions(state, actions);
       }

       getBotActions(state: GameStateEntity, botPlayerId: number) {
         return this.botStrategy.getBotActions(state, botPlayerId);
       }
     }

┌──────────────────────────────────────────────────────────────────────────┐
│ IMPORTANT - Sprint suivant (2 semaines)                                 │
└──────────────────────────────────────────────────────────────────────────┘

5. Nettoyer GameWsHandler (BACKEND)
   Priorité: ÉLEVÉE
   Temps estimé: 4 heures

   Actions:
   a) Vérifier si modules() est utilisé (grep dans client)
   b) Si non utilisé: supprimer méthode + 9 dépendances
   c) Si utilisé: créer GameModuleRegistry
   d) Ajouter tests unitaires
   e) Documenter

6. Éliminer duplications (BACKEND)
   Priorité: ÉLEVÉE
   Temps estimé: 1 jour

   Actions:
   a) Créer ArrayConverter.toStringArray()
   b) Créer StandFormatter.formatStandId()
   c) Créer PlayerNormalizer service
   d) Remplacer toutes les occurrences
   e) Supprimer code dupliqué
   f) Ajouter tests

7. Améliorer typage backend (BACKEND)
   Priorité: ÉLEVÉE
   Temps estimé: 2 jours

   Actions:
   a) Définir type ShoppingItem = string
   b) Définir type InventoryItem = string
   c) Remplacer unknown[] dans PlayerStateEntity
   d) Ajouter type guards
   e) Tester compilation stricte

8. Refactoriser GenericGameInteractionComponent (CLIENT)
   Priorité: ÉLEVÉE
   Temps estimé: 3-5 jours

   Phase 1 - Extraire GameStateViewModel:
     public class GameStateViewModel {
       private GenericGameState lastState;
       private List<String> shoppingView;
       private List<String> basketView;
       private List<String> inventoryView;

       private final List<Consumer<GenericGameState>> listeners =
         new CopyOnWriteArrayList<>();

       public void setState(GenericGameState state) {
         this.lastState = state;
         // Extract views
         notifyListeners();
       }

       public void addListener(Consumer<GenericGameState> listener) {
         listeners.add(listener);
       }

       private void notifyListeners() {
         listeners.forEach(l -> l.accept(lastState));
       }
     }

   Phase 2 - Extraire KeyboardBindingManager:
     public class KeyboardBindingManager {
       private final JComponent component;
       private final Map<String, Runnable> bindings = new HashMap<>();

       public void bind(String key, Runnable action) {
         bindings.put(key, action);
         component.getInputMap().put(
           KeyStroke.getKeyStroke(key),
           key
         );
         component.getActionMap().put(key, new AbstractAction() {
           @Override
           public void actionPerformed(ActionEvent e) {
             action.run();
           }
         });
       }

       public void unbindAll() {
         bindings.keySet().forEach(key -> {
           component.getInputMap().remove(KeyStroke.getKeyStroke(key));
           component.getActionMap().remove(key);
         });
         bindings.clear();
       }
     }

   Phase 3 - Composant principal devient coordinateur:
     public class GenericGameInteractionComponent extends JPanel {
       private final GameStateViewModel viewModel;
       private final KeyboardBindingManager keyboardManager;
       private final GameDialogManager dialogManager;
       // ... renderers

       public GenericGameInteractionComponent(...) {
         this.viewModel = new GameStateViewModel();
         this.keyboardManager = new KeyboardBindingManager(this);
         this.dialogManager = new GameDialogManager(this);

         initUI();
         initListeners();
         initKeyBindings();
       }

       @Override
       public void onState(GenericGameState state) {
         SwingUtilities.invokeLater(() -> {
           viewModel.setState(state);
         });
       }

       private void renderState(GenericGameState state) {
         statusRenderer.render(state);
         turnRenderer.render(state);
         actionsRenderer.render(state);
         // ...
       }
     }

┌──────────────────────────────────────────────────────────────────────────┐
│ SOUHAITABLE - Backlog                                                    │
└──────────────────────────────────────────────────────────────────────────┘

9. Validation stricte DTOs (BACKEND)
   - Ajouter @MaxLength, @Matches, @IsIn
   - Créer validateurs custom
   - Tester edge cases

10. Améliorer gestion erreurs (BACKEND + CLIENT)
    - Créer custom exceptions
    - Logger stack traces complètes
    - Catégoriser les erreurs

11. Rate limiting WebSocket (BACKEND)
    - Limiter messages/seconde par client
    - Throttling intelligent
    - Ban temporaire si abus

12. Monitoring et métriques (BACKEND + CLIENT)
    - Prometheus metrics
    - Grafana dashboards
    - Alerting sur erreurs

13. Tests unitaires (BACKEND + CLIENT)
    - Couverture > 80%
    - Tests d'intégration
    - Tests E2E

14. Documentation (BACKEND + CLIENT)
    - Architecture decision records (ADR)
    - API documentation (OpenAPI)
    - Diagrammes UML
    - Guide développeur

15. Performance (BACKEND)
    - Profiling
    - Optimisation requêtes
    - Caching stratégique
    - Connection pooling

16. Sécurité avancée (CLIENT)
    - Keystore système pour clé
    - Certificate pinning
    - Obfuscation code
    - Anti-tampering


================================================================================
10. STATISTIQUES
================================================================================

MÉTRIQUES GÉNÉRALES
-------------------

Backend:
  Fichiers analysés: 50+
  Langages: TypeScript (NestJS)
  Lignes de code: ~15,000+
  Bugs critiques: 3
  Bugs élevés: 5
  Duplications: 8 majeures
  Plus gros fichier: panier-express.service.ts (1040 lignes)
  Plus de dépendances: PanierExpressService (19 services)

Client:
  Fichiers analysés: 50+
  Langages: Java (Swing)
  Lignes de code: ~20,000+
  Bugs critiques: 2
  Bugs élevés: 3
  Duplications: 4 majeures
  Plus gros fichier: GenericGameInteractionComponent.java (1032 lignes)
  Plus de champs: GenericGameInteractionComponent (70+)

COMPLEXITÉ
----------

Backend - Fichiers complexes:
  1. panier-express.service.ts (1040 lignes, 19 dépendances)
  2. game-engine.service.ts (~300 lignes, logique complexe)
  3. panier-express-draw.service.ts (~150 lignes)
  4. game.gateway.ts (261 lignes, WebSocket)

Client - Fichiers complexes:
  1. GenericGameInteractionComponent.java (1032 lignes, 70+ champs)
  2. AbstractRealtimeService.java (~400 lignes, threading complexe)
  3. GenericGameInteractionController.java (~300 lignes)

DETTE TECHNIQUE
---------------

Estimation temps correction bugs critiques: 5-10 jours
Estimation temps refactoring complet: 3-4 semaines
Estimation temps tests unitaires: 2-3 semaines
Estimation temps documentation: 1 semaine

Total dette technique: ~2-3 mois de travail

POINTS POSITIFS
---------------

Backend:
  ✓ Architecture modulaire NestJS propre
  ✓ Bonne utilisation injection dépendances
  ✓ Séparation concerns au niveau modules
  ✓ WebSocket gateway bien implémenté
  ✓ Logging avec playing-logger
  ✓ Validation avec class-validator (partielle)

Client:
  ✓ Pattern MVP bien implémenté
  ✓ Reconnexion WebSocket excellente
  ✓ Internationalisation complète (540+ entrées)
  ✓ Chiffrement AES/GCM correct (sauf clé)
  ✓ Renderers bien encapsulés
  ✓ Threading généralement correct (sauf EDT)

PRIORITÉS
---------

Priorité 1 (URGENT):
  1. Leak mémoire SseLogger
  2. Race condition EDT
  3. Absence persistance
  4. Violation SRP PanierExpressService

Priorité 2 (IMPORTANT):
  5. God Component GenericGameInteractionComponent
  6. Duplications massives
  7. Refactoring game-ws incomplet
  8. Types dangereux unknown[]

Priorité 3 (SOUHAITABLE):
  9. Validation DTOs
  10. Gestion erreurs
  11. Tests unitaires
  12. Documentation
