# Preuves de clôture du lot de 130 corrections

Chaque ligne associe les exigences du snapshot du 12 septembre 2026 à un garde
exécutable et à son contrat d’architecture. Un point n’est retiré de
`corriger.txt` qu’après passage de ces contrôles sur le projet complet.

| Points  | Preuve maintenue                                                                                                                                                                                                                                                                                |
| ------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1–15    | Les trois catalogues d’extensions retirent les listes du document, du schéma et du compilateur génériques. `engine-extension-governance.cjs` classe les 39 profils, recalcule leurs consommateurs et bloque toute hausse des 1 144 lignes restantes. `TrackRaceProgram` mutualise onze courses. |
| 16      | `runtime-dependency-graph.spec.cjs` analyse tout `src/**/*.ts` et exige zéro composante fortement connexe.                                                                                                                                                                                      |
| 17–22   | `game-engine-architecture-check.cjs`, `architecture-check.cjs` et `runtime-separation-audit.cjs` interdisent le TypeScript de production dans les jeux, les imports runtime externes, les réexports d’infrastructure, les contrats fourre-tout et les dépendances ascendantes.                  |
| 23      | Les rôles portent désormais les noms distincts `RoomSnapshotProjection` et `VaultSnapshotCaptureInput`; le test de composition vérifie leur compatibilité exacte sans dépendance entre contextes.                                                                                               |
| 24–33   | La limite de 300 lignes, le chargeur `$content`, le schéma fermé, les validateurs de références/capacités et l’audit du moteur garantissent la modularité, les IDs et l’absence de code arbitraire.                                                                                             |
| 34–41   | `json-version-policy.spec.ts`, `engine-snapshot-migrations`, `game-state-loader` et `json-restored-session.spec.ts` couvrent les trois versions, la version du snapshot, les migrations déterministes et la validation avant restauration.                                                      |
| 42–51   | `content-immutability`, `setup-state-isolation`, `assert-serializable-state`, les tests selectors/RNG/ranking, `automatic-stabilization` et l’exécuteur transactionnel couvrent immutabilité, isolation, ordre, hasard, boucles, atomicité et pureté.                                           |
| 52–60   | `game-automation-delivery.md`, les tests BullMQ/reconciliation/CAS et `realtime-request-replay.service.spec.ts` couvrent échec après commit, rejeu, jobs obsolètes, restauration, concurrence, stale writes, séquences, resynchronisation et collision de request ID.                           |
| 61–65   | `security-boundary-audit.cjs`, les guards WS et les tests de visibilité imposent l’identité authentifiée, l’autorisation par commande, les projections en whitelist et la séparation debug/joueur.                                                                                              |
| 66–68   | Les erreurs du domaine n’appellent plus `new Date()`; `business-clock-lint.spec.cjs` et les réglages opérationnels séparent horloge métier, mesure technique et TTL nommés.                                                                                                                     |
| 69–75   | Les contrats Presence multi-instance, Pub/Sub éphémère, caches non autoritatifs, événements versionnés et consumers idempotents sont contrôlés par les tests Redis/BullMQ, l’audit des retries et les contrats d’événements. La décision outbox/inbox est documentée par type de flux.          |
| 76–81   | Les services Update bornent flux, temps, mémoire et répertoires, inspectent ZIP/PE, vérifient signature et SHA-256, publient par renommage atomique et distinguent bail distribué et lock local. Les releases historiques restent immuables.                                                    |
| 82–91   | Les audits sécurité, observabilité et opérabilité vérifient redaction, cardinalité, liveness/readiness, arrêt gracieux, rate limiting Redis, rotation des refresh tokens, secrets sans fallback, quotas et validation runtime.                                                                  |
| 92–100  | Les audits de persistance et l’historique SHA-256 des migrations imposent ports sans ORM, read models, owner unique, relations born N+1 revues, migrations immuables/autonomes, DDL borné et retries seulement idempotents.                                                                     |
| 101–112 | Les contrats d’architecture gardent `shared` minimal, les primitives dans le moteur, suppriment les pass-through et imposent les conventions de noms/null/date. Les kits Resources, Inventory, Cards, Movement et Ranking possèdent leurs invariants.                                           |
| 113–122 | Le test du quarantième jeu prouve la création JSON seule; le catalogue exige une mécanique motivée, mesure les consommateurs et le ratio générique/spécifique. Les audits duplication, dead code et surface SDK empêchent le retour des loaders, wrappers et exports inutiles.                  |
| 123–128 | `quality:check` exécute les passes finales ownership, sérialisabilité, déterminisme, versions, frontières externes, observabilité et comportement de production.                                                                                                                                |
| 129     | `production-readiness.md`, l’artefact de déploiement et les intégrations réelles couvrent lockfile, ESLint/TSConfig, CI, Docker, DB, Redis/BullMQ et sauvegardes au-delà de l’ancienne archive.                                                                                                 |
| 130     | `generate-game-registry.spec.cjs` crée quarante jeux JSON avec les mécaniques existantes et vérifie 40 compilations sans aucun fichier TypeScript auteur.                                                                                                                                       |

Le garde `maintained-invariants-audit.cjs` vérifie que ces preuves restent présentes
et que chaque audit critique appartient toujours à `quality:check`.
