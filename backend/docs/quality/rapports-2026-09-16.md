# Revue des neuf rapports du 16 septembre 2026

Lecture de tous les rapports et de leur commentaire associé en base, sans
modifier les statuts ni supprimer les rapports. Les validations ci-dessous
sont automatisées ; une compilation Windows ne remplace pas une séance NVDA.

| Rapport | Traitement et vérification |
| --- | --- |
| `65c861e4` — tournoi | Demande de nouvelle rubrique déjà refusée, pas un bug reproductible. Décision conservée : aucune rubrique ni ambiance ajoutée sans réouverture explicite. |
| `ebce8fd2` — rejoindre une personne | Ajout de « Rejoindre sa table » dans Présence pour un autre joueur situé à une autre table. Utilise le parcours normal et les contrôles d'accès du serveur, sans exigence d'être propriétaire de sa table actuelle. |
| `14739894` — Contes et cacahuètes | Tests de régression : deux cartes du Coffre, interruption par Poussière de rire sur plusieurs graines, Pont avec Formule magique puis Livre à l'envers, échange des prochains tours, recul et passage du joueur bloqué par le Loup. Ces scénarios passent sur le moteur actuel sans modification des règles. Le texte des cartes titrées est désormais conservé dans l'annonce. La demande imprécise de changer S n'est pas transformée arbitrairement en une autre commande. |
| `83be3895` — Cat Pattes | Sol ciré autorise les cartes de 10, 20 et 50, mais pas les plus grandes. Après une parade autre que Rayon, le soleil doit être réactivé (Passage de star reste prioritaire). Tests des cinq couples obstacle/parade et de la limite Turbo-chat. Règles et texte Sol ciré mis à jour ; contenu version 2 avec migration compatible depuis 1 et depuis l'ancienne version TypeScript. |
| `4ef4937b` — Frousse Party | Une carte dont le texte décrit déjà l'effet n'est plus suivie d'une paraphrase technique automatique. Test sur la carte « Le sol colle à vos chaussures ». |
| `a75dcb50` — content / test | Aucun dysfonctionnement décrit. Rapport conservé, aucune correction inventée. |
| `0afdb2b1` — envoi messagerie | L'échec du compteur de notifications ne doit plus faire échouer la réponse après enregistrement du message. Test de panne des notifications et compteurs. Le champ de message n'intercepte plus Tab comme une tabulation de texte. |
| `5d691420` — mon profil | Correction de sérialisation des dates déjà présente dans la version 1.2.185. Suite de sérialisation profil et tests sociaux relancés. Pas de nouvelle modification inutile du profil. |
| `5f608a26` — Morpion | Les événements de pose de pion reçoivent une annonce française (joueur, ligne, colonne). La grille affiche des coordonnées humaines à partir de 1. Tests des coups, victoire, cases occupées et conservation des traces existantes. Les chansons ne sont pas identifiées par le rapport : précision demandée, aucune chanson inventée ou ajoutée. |

## Limites conservées explicitement

- Pas de clôture automatique des rapports : notamment les chansons du Morpion
  et l'évolution souhaitée de S dans Contes restent à préciser.
- Les parcours de focus et le comportement NVDA doivent être confirmés sur le
  client Windows. Aucun envoi de message privé de test aux vrais utilisateurs.
- Les tests reproduisent les scénarios décrits ; ils ne garantissent pas que
  toutes les anciennes parties sauvegardées reproduisent exactement ces états.

## Validation avant publication

- 19 suites backend ciblées, 95 tests réussis (jeux, narration, contenu,
  migrations, messagerie et profil).
- Vérification TypeScript sans erreur.
- Vérification syntaxique MinGW des six fichiers C++ d'interface modifiés.
- Suite portable client réussie, y compris la résilience réseau. Son script
  inclut désormais le fichier heartbeat, auparavant oublié dans l'édition de liens.
