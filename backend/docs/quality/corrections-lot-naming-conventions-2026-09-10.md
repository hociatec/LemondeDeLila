# Verification of point 18

The module convention now gives stable meanings to `Entity`, `Model`, `Record`, `DTO`, `Command`, `Query`, and `Port`, and requires responsibility-oriented names for other forms.

Evidence: `docs/architecture/module-conventions.md`, section `Vocabulaire et tests`.
