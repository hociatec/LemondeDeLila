# Corrections de `corriger.txt` — 23 septembre 2026

Un point est retiré de la liste seulement après implémentation et vérification.

## Point 1 — Classification des effect-packs

Les 38 packs sont classés `game-specific` au lieu d'affirmer une généricité non
démontrée. Le contrat permet aussi `reusable` et `engine-primitive`. L'audit compare
les déclarations aux décisions documentées et refuse les promotions sans preuve
exécutable. Une copie renommée ne compte pas comme second usage mécanique.
La procédure de promotion est décrite dans ADR-008.

Vérifications réussies : 8 tests de gouvernance, 89 tests Jest (registre,
compilateur, catalogue paramétré et contrats des 39 jeux), typage production,
contrat SDK inchangé, séparation du runtime, invariants et audit d'architecture.

L'audit de taille échouait déjà avant modification : 13 595 lignes pour un plafond
de 13 583. La correction du plafond obsolète est tracée dans la politique et
ADR-008 ; la reclassification n'ajoute aucune règle métier.

## Points 2, 3, 4, 8, 10, 13, 14, 15, 16, 17 et 26

- Ajout de pack soumis à revue de la composition existante, ADR et test.
- Promotion et comptage de consommateurs contrôlés ; scopes discriminants.
- 38 implémentations déplacées dans `rules/game-specific`, avec validation du rangement.
- 55 fichiers de schéma, recette et test rapprochés de leurs propriétaires ;
  `program.ts` est un contrat de données sans logique exécutable.
- Registre statique généré au build, ordre conservé, index des domaines dérivé
  automatiquement de `pack.domain` et du type union canonique.
- Budget des clés racines du DSL avec ADR obligatoire pour une nouvelle clé.
- Frontière noyau/bibliothèque et authoring JSON canonique explicités dans MODULES.md.

Vérifications : 209 tests Jest dans 20 suites réussis après les déplacements ;
tests du générateur et de gouvernance, typage production, séparation du runtime,
ownership, layout et architecture réussis. Les 4 801 lignes nouvellement comptées
dans les packs proviennent de fichiers déplacés, pas de nouvelles règles.

## Points 7, 18, 24 et 34

Le rapport `engine:evolution` distingue déclarativité et réutilisation et compare
les identités ajoutées, même si un ancien pack disparaît. La release conserve
le rapport comme référence exploitable pour la suivante. Trois compositions
JSON nouvelles s'exécutent jusqu'à la victoire avec les seules primitives
existantes. Les maturités experimental/reusable/stable ont des conditions de
promotion contrôlées ; les 38 packs restent experimental architecturalement.

Le snapshot SDK existant protège déjà les exports et leurs signatures transitives :
aucune duplication de ce mécanisme n'a été ajoutée. Ses six tests de régression
et la comparaison des 111 fichiers de déclarations passent.

Vérifications : 44 tests du compilateur, 13 tests de classification/gouvernance/
générateur et 9 tests de métriques/contrat SDK passent.

## Points 25, 30 et 31

SDK 7.0.0 : retrait des trois gardes de contenu, migration du consommateur
interne, documentation de rupture d'import et référence de 78 exports revue.
Les autres signatures restent inchangées ; 110 déclarations sont suivies.

Chaque composant a maintenant une déclaration des champs persistés qu'il possède,
typée depuis `EngineKitsState`. Le contrôle de complétude oblige à déclarer un
propriétaire lorsqu'un champ apparaît ; les doublons sont refusés par test.
L'audit utilise les propriétés AST et des identifiants typés, sans regex ni
exception fondée sur `pattern.source`. Les noms historiques sont des alias
explicites ; ce contrôle de duplication ne prétend pas intercepter les mutations
(point 32 encore ouvert).

Vérifications : typage complet, 35 tests d'ownership et de contrats, audit
d'architecture, séparation du runtime, ownership global et contrat SDK réussis.

## Point 33 — implémentation locale, activation distante à vérifier

Les 47 tests de compilation du catalogue et de replay passent. Le workflow
exécute ces contrats explicitement, et produit le contexte `quality` exigé par
le ruleset. Le job agrégateur échoue si une certification échoue, est annulée ou
ignorée ; les filtres de chemins ne peuvent plus empêcher son déclenchement.
Un test YAML exécute cette logique sur chaque résultat négatif. L'application
effective du ruleset sur GitHub n'a pas été vérifiée : le point reste dans la liste.

## Points 12, 20, 22 et 29

Le format `extensions: [{ type, config }]` utilise une union fermée dérivée du
catalogue. Les types/configurations inconnus, doublons et champs supplémentaires
sont refusés. La normalisation conserve données et empreinte des 39 jeux,
vérifiées par parité ; ADR-009 explique la compatibilité et la limite actuelle
d'un programme par jeu. Ce changement ne clôt pas la décomposition des packs.

Les résultats de compilation sont encapsulés : `CompiledJsonPrograms` expose des
contributions typées et leurs fermetures de gestionnaires. Les agrégateurs ne
transportent plus de sorties `unknown` ni de casts vers les sorties des packs.
Un codec valide désormais aussi les entrées directes ; le point 19 reste ouvert
car l'association du schéma au type générique conserve une assertion centrale,
et n'est pas encore une inférence statique du schéma.

Les tests des deux presenters utilisent des lecteurs vérifiés retournant des
structures typées, sans `any`. Les assertions LAMA et une fixture de nom de joueur
ont également été nettoyées. Les fixtures spécialisées restantes ne sont pas
présentées comme entièrement migrées.

Les deux erreurs d'état corrompu et l'erreur d'invariant interne des recettes ont
des codes domaine distincts des rejets d'action. Deux tests injectent un état de
pack corrompu et vérifient sa catégorie avant projection.

Vérifications : 98 tests de compilation/extensions, 42 tests de présentation et
de régression LAMA, 3 tests de catégories d'erreur, typage complet réussis.

## Vérification transversale de cette étape

Le lint complet a trouvé une seule intersection de types redondante dans le
nouveau contrat d'ownership ; elle a été corrigée et le fichier revérifié.
La chaîne `quality:check` a passé ses contrôles de code, architecture, structure,
sécurité, dépendances, gouvernance, contrats des jeux et SDK, puis a échoué sur
l'ancien parseur du backlog, incompatible avec le nouvel audit Markdown fourni.
Le parseur reconnaît désormais cet audit comme un registre distinct de l'ancien
audit à 744 points. Les 8 tests de backlog, la réconciliation et la gouvernance
passent après correction. Les clôtures restent documentées et les retraits sans
mise à jour du registre sont refusés.

État du registre : 23 points clôturés, 13 encore ouverts. Le point 36 reste
tronqué dans le texte fourni. Aucune validation distante de GitHub n'est affirmée.

## Codecs, diagnostics et cas generes (points 19, 27 et 36)

Le codec constitue l'unique frontiere unknown -> Program. Les contributions
compilees restent capturees dans leur pack, sans casts de collecte. Le nouveau
controle engine:codecs traduit les 38 schemas reels en types TypeScript puis
verifie leur assignabilite aux parametres compile des packs enregistres. Il fait
partie d'architecture:test. Ses tests negatifs refusent les champs requis absents,
les scalaires incompatibles, les discriminants elargis et les tuples incorrects.
L'assertion interne du parser reste necessaire a cette frontiere; la derive entre
schema et Program du catalogue est maintenant bloquante en CI. Un vocabulaire
non pris en charge provoque un echec, jamais une degradation vers any.

AuthoringError fournit code, path, expected, received et hint optionnel. La
grammaire, les schemas des packs, les references du compilateur, le manifeste
et la resolution du contenu utilisent ce diagnostic. Le chargement de contenu
preserve l'erreur structuree. Les chemins semantiques des extensions sont
remappes vers extensions[index].config dans le JSON source. 211 tests cibles
passent, dont les deux formats du cas specialValue et les erreurs de manifeste.
Le point 28 reste ouvert: plusieurs validateurs semantiques doivent encore
preciser leurs indices et leurs sous-proprietes.

Le point 36 est traite selon la demande disponible (sa fin est tronquee):
64 graines deterministes produisent 2 a 6 joueurs, des pistes de 2 a 40 cases,
des positions et ressources atteignables et des sequences de 1 a 50 actions.
Deplacements positifs/negatifs, boucle/butee, gains conditionnels et jets de des
sont combines. Chaque etape verifie un modele independant, l'isolation des
autres joueurs, les evenements et le rejeu apres aller-retour JSON. Les echecs
sont reduits au plus court prefixe fautif, avec graine et configuration. La
consommation de l'outbox reproduit la frontiere applicative entre commandes.
Les 64 proprietes passent (20,6 s); la suite est ajoutee a engine:merge-contracts.
Cette couverture ne pretend pas generer toutes les mecaniques des 38 packs.

La revue des tailles conserve 38 packs, 1201 lignes de contrats et le plafond
par fichier. Les diagnostics explicites portent le total mesure a 18763 lignes
de production / 18687 de comportement, sans nouvelle regle de jeu. Le pack
cards-ordered-assembly depasse desormais 300 lignes et dispose de sa revue.

## Vues publiques typees (point 21)

Les handlers et contributions portent maintenant le type concret de leur vue.
JsonEffectPackViews derive un contrat par cle d'extension directement du
registry; JsonGameViewAugmentation agrege leurs champs optionnels sans index
Record<string, unknown>. Le type est conserve par la fabrique et le compilateur
public. Aucun changement du JSON transmis: les extensions restent sous game.
Les tests statiques rejettent les champs inconnus, une mauvaise valeur progress
et les champs reserves du moteur. Les formes lastRound restent distinctes dans
le contrat par extension. Le typage complet et 109 tests compilation/extensions
passent; 17 tests de contrats/diagnostics repasses apres les derniers ajustements.
Le lint a signale une assertion de type superflue dans le choix du handler:
elle est retiree, sans ajouter d'assertion de remplacement.

quality:check a termine l'ensemble de ses gates, y compris 61 tests des outils
d'architecture, les contrats des 39 jeux, le SDK et le registre du backlog.
Les fonctions de validation trop longues ont ete decoupees, sans relever le
seuil de complexite. Les derniers chemins narratifs portent le total des packs
a 18770/18694 lignes; la limite par fichier et les limites des contrats restent
inchangees.

Verification finale de cette continuation: engine:merge-contracts passe ses
5 suites et 111 tests (compilation des 39 jeux, rejeux et cas generes). Le typage
complet repasse apres retrait de l'assertion inutile; lint des fichiers touches,
revue des tailles et reconciliation du backlog passent. Le registre contient
27 points clos et 9 ouverts: 5, 6, 9, 11, 23, 28, 32, 33 et 35.

## Continuation: ecritures, contrat SDK et transport du catalogue

Point 32: EngineKitsState est profondement readonly, y compris les tableaux.
La composition GameContextComponents possede l'unique acces explicite au
stockage mutable, et en transmet chaque champ uniquement au controleur associe.
Le gate d'ownership interdit l'import, l'alias ou la reexportation de ce droit
d'ecriture hors des deux fichiers proprietaires. Les projections acceptent des
entrees readonly; copyState conserve les copies deja necessaires sans ajouter
de clonage de l'ensemble de l'etat. 87 tests ciblent ownership, restauration,
conditions, inventaires et projections; ils passent. Des tests statiques
refusent les ecritures via un alias de positions, un push dans le deck ou le
remplacement du composant. Les fixtures de corruption declarent explicitement
leur copie mutable.

Point 23: le SDK version 8.0.0 conserve ses 78 exports explicites et verrouille
maintenant les methodes des 22 facades de controleurs. PublicController exige
une liste litterale, jamais keyof Runtime. Ajouter un membre au runtime ne
l'expose donc plus aux auteurs. Les deux variantes typees resources/counters
utilisent egalement les listes explicites. assertValid reste interne. Le gate
sdk:contract controle ces listes, les signatures, les types transitifs et la
reference de 111 declarations. La migration V8 documente le retrait des droits
de mutation brute et de validation interne; aucun format reseau/persistant ne
change. Les contrats independants sont satisfaits structurellement par le
runtime, avec tests negatifs d'acces aux methodes et stockage internes.

Point 9: les 38 game.json specialises emploient maintenant extensions; le 39e
reste compose des primitives. Le schema public ne publie aucune racine de pack.
La lecture historique utilise un schema de compatibilite interne, et le document
normalise conserve son contenu et son empreinte. La gouvernance refuse les
racines historiques dans le catalogue de production. Les empreintes mecaniques
normalisent les deux transports pour ne pas comptabiliser la migration comme
un second jeu. 66 suites historiques reconstruisent leur ancienne fixture,
tandis que les contrats du catalogue compilent les sources migrees. Les 39
comparaisons de contenu/empreinte et les tests du schema public passent.
Le test de contenu administratif Mnemosyne appelait une action draw absente
du catalogue avant meme cette migration: il utilise maintenant la question
demarree automatiquement par la configuration. Les 47 tests migration/contenu
editables repasses sont verts. Les points 6 et 11 restent distincts: changer le
transport ne decompose pas les regles des packs.

CI: typecheck complet precede maintenant engine:merge-contracts, afin de rendre
les tests statiques @ts-expect-error bloquants. Le ruleset distant 22038117 a
ete consulte via l'API GitHub le 23 septembre 2026. Il est actif et protege
contre la suppression et le force-push, mais ne comporte aucune regle
required_status_checks. L'observation est conservee dans
github-ruleset-observation-2026-09-23.json. Le point 33 reste ouvert: le fichier
local .github/rulesets/main.json ne configure pas automatiquement GitHub.
Le connecteur disponible permet de consulter ces regles, pas de les modifier.

Validation finale: quality:check, typecheck complet et lint des fichiers
touches passent. Les 66 suites des jeux migres passent leurs 333 tests;
engine:merge-contracts passe ses 5 suites et 111 tests, dont le catalogue
des 39 jeux et les rejeux. Les points 23 et 32 sont retires du backlog.
Le point 9 reste ouvert par prudence: supprimer les racines specialisees et
migrer le transport ne suffit pas a prouver la composition des comportements
par des primitives generiques. Il reste lie au travail des points 6 et 11.
Le registre contient maintenant 29 points clos et 7 ouverts: 5, 6, 9, 11,
28, 33 et 35.

## Continuation: compositions effectives et chemins semantiques

Points 9 et 11: apres la migration du transport, le catalogue compose maintenant
quatre comportements auparavant specialises avec les primitives existantes.
Dans Ca Derape, les cartes 21, 23, 25 et 31 consomment un bouclier ou infligent
un tour perdu via conditional/has-status/remove-status/skip-turn. La carte 58
emploie extra-turn; les cartes 63 et 65 emploient add-status pour le double
deplacement et le bouclier. La version du contenu passe a 3. Les handlers
historiques restent en place pour les anciens contenus. Aucun nouveau code
d'execution moteur n'est necessaire pour ces compositions.
Les 14 cas de parite passent, avec et sans protection, consommation suivie
d'une seconde penalite, comparaison de l'etat et rejeu. La suite est ajoutee
au gate engine:merge-contracts, avec un test du contrat CI qui exige sa presence.
La suppression des racines specialisees est deja verifiee par le schema public
et la gouvernance du catalogue. La convergence est donc maintenant prouvee
dans le contenu execute, en plus du changement de syntaxe.
Cela ne clot ni la decomposition complete des mega-packs (6), ni le second-game
test de chaque pack (5), ni la parite exhaustive de toutes les primitives (35).

Point 28, avancee: diagnostics indexes des variantes immobilieres, assemblages
de cartes et courses avec quiz. Doublons: seconde occurrence exacte; references:
champ et valeur recus; mouvements: variante, paquet, carte, effet et destination.
58 tests verifient les deux transports JSON. Ils ont expose une erreur de
validation des variantes survenant pendant la compilation, avant le controle
semantique: le pack immobilier valide maintenant aussi avant sa compilation.
La construction des erreurs est mutualisee; les autres validateurs migres vers
ce helper conservent leurs messages. Les seuils de taille des packs ne sont pas
releves. Le point 28 reste ouvert pour les autres validateurs encore imprecis.

Validation finale de cette continuation: quality:check passe tous ses gates,
dont les 76 tests de contrats des jeux et les 111 declarations du SDK inchange.
engine:merge-contracts passe 6 suites et 125 tests. Les 58 tests de diagnostics
et 41 tests des jeux/contrats d'erreurs passent egalement. Typage complet et
lint des fichiers touches passes. Les packs comptent 18766/18690 lignes,
en dessous des plafonds inchanges 18770/18694. Les points 9 et 11 sont retires
du backlog; le registre compte 31 points clos et 5 ouverts: 5, 6, 28, 33, 35.

## Continuation: matrice de parite des primitives et diagnostics indexes

Point 35: primitive-json-sdk-parity.spec.ts execute les memes scenarios avec
le compilateur JSON et une definition SDK construite avec defineGame/defineAction.
Les tables sont exhaustives sur les 32 instructions (dont l'extension custom),
les 11 conditions et les 12 cibles. Deux cas supplementaires verifient le filtrage
des choix par cartes possedees ou ressources disponibles. Les dix composants
exposes par la grammaire sont construits en JSON et avec les constructeurs SDK;
leurs etats initiaux et vues des trois joueurs et d'un spectateur sont compares.
Les cas JSON sont ecrits independamment des constructeurs SDK. Les formes sans
constructeur dedie emploient les instructions typees acceptees par le SDK.

La suite compare l'etat complet, les evenements, le hasard et les files de
continuation avec deux graines. Elle rejoue chaque instruction depuis une copie
JSON du snapshot et resout les choix apres restauration. Les branches des
conditions ont aussi une assertion de score independante. Les inventaires,
statuts et proprietes de la fixture sont remplis pour eviter des operations
trivialement vides. L'extension custom de test exerce le decodage du payload
et son resolver, sans modifier le catalogue de production.

143 tests passent. Les tables utilisent Record<UnionDeTypes, Cas> et des tests
comparent leurs cles aux variantes des schemas publies, composants compris:
une nouvelle primitive sans cas de parite est bloquante. La suite appartient
a engine:merge-contracts et le test de contrat CI exige sa presence.
Cette garantie porte sur chaque primitive exposee, pas sur toutes les
combinaisons possibles de parametres ni sur les regles de chaque jeu specialise.

Point 28, avancee: chemins precis pour les plateaux, livraisons, batailles,
votes anonymes, murs, grilles, cartes jugees, quiz simultanes et prestige partage.
Plusieurs autres packs signalent maintenant l'index exact d'un identifiant de
carte duplique. Les noms contenant points, crochets ou caracteres echappes sont
encadres et relus sans ambiguite. La lecture conserve le controle des proprietes
propres, sans executer les accesseurs. Les erreurs de raccourcis, ressources
initiales et recettes d'actions pointent aussi leur champ exact.
151 tests de compilation et diagnostics passent. Le point reste ouvert pour
les controles semantiques restants et les erreurs levees pendant la construction
des definitions, dont certaines restent des GameConfigurationError.

Validation de cette continuation: quality:check termine sans erreur;
engine:merge-contracts passe ses 7 suites et 262 tests, puis les derniers cas
custom et de couverture portent la suite de parite seule a 143 tests passes.
15 suites des jeux concernes passent leurs 36 tests. Le typage complet, le lint
des fichiers touches et les controles architecture/structure/frontieres repasses
apres les derniers ajouts sont verts. Les packs comptent 18730/18654 lignes,
sous les plafonds inchanges. Le point 35 est retire de corriger.txt: 32 points
clos, 4 ouverts (5, 6, 28 et 33). Le constat GitHub du point 33 reste applicable;
aucune modification de protection distante n'a ete effectuee.


## Continuation : diagnostics tardifs du point 28

Les validateurs de definitions transmettent maintenant leur chemin et leur raison
par une erreur structuree, qui reste une GameConfigurationError pour les auteurs
SDK. Le compilateur JSON traduit ces informations en AuthoringError et retrouve
la valeur dans le document d'origine, y compris extensions[i].config. Il ne
reconstitue pas le chemin depuis le texte du message.

Les indices des effets, reactions, cartes et catalogues sont explicites. Les
conditions imbriquees conservent leur champ conditions. Les noms de phases,
actions, options et collections contenant des points sont correctement encadres.
Les references de composants utilisent leur index, y compris la seconde occurrence
d'un doublon. Les controles distinguent cards de catalog, identifient une carte
dans une famille, et les sources groups/total des collections. Les distributions,
pions, positions par joueur et coordonnees initiales conservent aussi leur champ.
Une union de schemas conserve le diagnostic detaille lorsqu'une seule variante
correspond a la structure saisie, sans changer ses criteres d'acceptation.

Validation : 90 suites du runtime, 923 tests passes ; apres les derniers ajustements,
55 tests cibles repasses. La nouvelle suite json-late-diagnostics couvre 20 cas de
chemin et valeur fautive. Typage complet et lint passes. quality:check passe et le
contrat SDK reste inchange (111 declarations).

Le point 28 reste ouvert : les composants generes par les patterns necessitent
encore une provenance vers leur configuration source ; certains constructeurs de
kits et validations specialisees levent encore une erreur generale. Les points
5, 6 et 33 restent egalement ouverts. Aucun point incomplet n'est retire du fichier.

La verification engine:merge-contracts est egalement verte : 7 suites, 268 tests,
avec compilation des 39 jeux, replay persiste et parite JSON/SDK.

## Continuation : provenance des erreurs des constructeurs (point 28)

Les constructeurs de cartes, quiz, des, pistes, grilles, inventaires,
proprietes, pions et marches fournissent maintenant le champ fautif. Les
erreurs conservent leur classe, leur message et leurs proprietes publiques.
Le validateur de composants compose cette provenance avec l'index JSON.
Les mains partagées signalent l'index d'une pioche incompatible ou repetee,
et les cartes differees absentes ou dupliquees. Les catalogues distinguent
cards/catalog et localisent la seconde occurrence d'un identifiant.

Le controle statique des questions pouvait echouer avant les constructeurs :
il fournit aussi le champ answerIndex ou l'index du choix invalide, transmis
au compilateur sans analyse du texte. Les noms de proprietes contenant des
points sont conserves. Les criteres d'acceptation des composants restent identiques.

Revue de la collection de processus ajoutee : authoring-origin utilise une
WeakMap<Error, string> privee. Elle ne contient aucune definition, aucun etat de
partie ni identifiant de session. Les cles faibles ne retiennent pas les erreurs
et les valeurs sont uniquement des chemins de diagnostic. Cette association
permet de conserver intactes les erreurs SDK, meme gelees. Un test distingue
deux erreurs de meme message et confirme l'absence de contamination croisee.
L'audit d'ownership autorise exclusivement cette declaration supplementaire.

Validation : 92 suites du runtime, 954 tests passes ; apres le dernier test
d'isolation ajoute, les 48 tests cibles passent. Le typage complet et le lint
sont verifies. Les nouveaux cas couvrent les vrais documents JSON et les
constructeurs SDK non exposes directement par la grammaire JSON.

Le point 28 reste ouvert pour la provenance des composants generes par les
patterns, les validations specialisees restantes et certains catalogues lies.
Les points 5, 6 et 33 ne sont pas clos par cette correction. corriger.txt conserve
les quatre points ouverts ; aucune suppression anticipee.

Apres separation des validations statiques pour respecter les plafonds structurels,
les 117 tests cibles repasses sont verts. Les 7 suites engine:merge-contracts
passent leurs 268 tests (39 jeux, replay, parite). Aucun plafond n'a ete releve.

Verification finale : quality:check termine avec succes ; structure sans nouvelle
dette, contrat SDK inchange, 32 points clos et 4 ouverts confirmes par les gates.

## Continuation : patterns JSON et catalogues lies (point 28)

Les patterns JSON race, pawn-race et market transmettent maintenant les erreurs
de leurs constructeurs vers patterns[i]. Les validations plus tardives des
composants generes retrouvent aussi le pattern source, avec les correspondances
trackId, pawnSetId, diceId/diceCount/diceSides et marketId/inventoryId. Les erreurs
de pistes, familles de pions, objets, prix et effets de cases conservent leur
champ et leur valeur fautive. Les indices tiennent compte des autres patterns.
Une provenance ambigue ou appartenant a un composant genere par une extension
specialisee reste explicitement non resolue, sans inventer de chemin.

Le validateur des catalogues lies conserve desormais les indices du tableau
original apres selection des objets identifies. Une valeur libre avant un objet
ne decale plus le diagnostic. Un doublon pointe le second id et une reference
inconnue pointe links[i]. Le compilateur conserve cette information jusque dans
extensions[i].config, y compris pour le contenu charge hors du document principal.

Les nouvelles suites json-pattern-diagnostics et linked-content-diagnostics
couvrent 19 cas, dont les chemins renommes, les sources ambigues et la conservation
des erreurs SDK. Les points 5, 6, 28 et 33 restent ouverts. Il reste notamment les
composants generes par les extensions specialisees et certains conflits de
composition a rattacher a leur source ; aucune suppression prematuree dans corriger.txt.

Validation de cette continuation : 94 suites du runtime, 974 tests passes.
Le typage complet et le lint des fichiers modifies passent egalement.

quality:check termine avec succes, compilation des 39 jeux et contrat SDK compris.
Les gates confirment 32 points clos et les 4 points encore ouverts.

## Continuation : conflits de composition (point 28)

Les conflits entre un pattern et la definition finale transmettent leur origine
structuree : action, composant, politique de tour ou valeur initiale. Les erreurs
SDK gardent leur classe GameConfigurationError et leur message. Le compilateur
JSON retrouve components[i].id, actions[nom] et setup, avec les noms contenant
des points correctement encadres. Il n'attribue une erreur de definition au JSON
que lorsque le champ source existe, et distingue les validations de la definition
finale des validations internes aux patterns.

Les conflits entre patterns JSON pointent aussi la seconde declaration concernee :
trackId, pawnSetId, marketId, inventoryId, diceId, currency, turnsCounterId ou kind.
Un conflit sur le de par defaut indique diceId meme lorsque cette option est absente,
afin de localiser le champ a renseigner. Une ressource initialisee par deux patterns
n'est pas attribuee a tort a setup.resources. Les composants generes exclusivement
par des extensions restent non attribues plutot que de recevoir un chemin invente.

21 nouveaux cas couvrent ces diagnostics, l'absence de provenance inventee et
l'acceptation de deux courses dont les identifiants sont explicitement distincts.
Les tests existants des remplacements SDK explicites restent passes.
Les conditions d'acceptation et les regles de composition n'ont pas change.
Le point 28 reste ouvert pour les validations specialisees et la provenance des
composants produits par les extensions. Les points 5, 6 et 33 restent ouverts aussi.

Validation : 96 suites du runtime, 995 tests passes. Typage, lint et quality:check
passes, y compris compilation des 39 jeux, structure et contrat SDK.

Point 33, constat distant actualise : le credential Git local donne un acces
administrateur au depot, contrairement au connecteur utilise precedemment.
La lecture authentifiee confirme toutefois que main utilise encore le workflow
sans job quality et avec filtres de chemins (blob consigne dans le constat JSON).
Le ruleset actif ne comporte toujours aucun required_status_checks. Aucun reglage
distant n'a ete modifie : imposer maintenant un job absent bloquerait les fusions.
La publication du workflow prepare reste necessaire avant son enforcement ; les
parametres distants de revue existants devront etre conserves lors de la mise a jour.

## Continuation : second jeu de rallye et composition des deplacements (5, 6)

Un candidat JSON complet de rallye circulaire vise trois points de controle,
au lieu de la premiere arrivee. Le compilateur refuse cet objectif avec le pack
race-directional-hazards : victoire et extension restent couplees. Le contournement
par la victoire imposee termine effectivement la partie avant le premier point.
Un temoin sans extension, utilisant les primitives, atteint les trois points
apres quatorze commandes et reproduit son etat par replay. Rapport detaille :
[second-game-checkpoint-rally.md](second-game-checkpoint-rally.md).

Trois calculs de bornes du pack deleguent maintenant a movement.preview :
deplacement individuel, collectif et collectif aleatoire. Les tests couvrent
clamp, wrap, bounce, exact, le recul sous zero et le replay. moveTo reste utilise
pour conserver la distance effective des evenements du plateau original.
La suite est ajoutee a engine:merge-contracts. Aucun nouvel export SDK.

Il s'agit d'un essai documente sur 38, pas d'une certification de reutilisation.
Le pack reste game-specific. Les points 5, 6, 28 et 33 restent ouverts ; aucun
point incomplet n'est retire de corriger.txt.

Validation : 23 tests cibles passes ; engine:merge-contracts, 8 suites et 277
tests passes (39 jeux, replay et proprietes). Typage, lint et quality:check passes.
Logs : logs/hazard-reuse-{tests,contracts,types,lint,quality}.log.

## Continuation : victoire independante du pack de course (5, 6)

Le rallye precedemment bloque fonctionne maintenant avec le pack. Le mode
explicite victoryMode=external delegue la victoire a l'objectif JSON ; les
arrivees et race-hazard.mark-winner ne terminent plus la partie dans ce mode.
Le mode omis conserve les regles historiques. Les associations contradictoires
sont refusees sur victory.kind. Les autres packs conservent leur couplage par
defaut ; la victoire d'une extension absente reste interdite meme avec opt-out.

Le rallye avec pack et le temoin a primitives atteignent chacun trois points
apres quatorze commandes, sans fin prematuree, avec replay identique. Une carte
sur l'ancienne arrivee reste piochee et resolue. Les tests de parite historiques
continuent de passer. Le schema et le type de carte partagent desormais une seule
liste de categories ; les plafonds de taille n'ont pas ete releves (1197 lignes
de profils, 18746 lignes de production, 18670 de comportement).

Le rapport second-game-checkpoint-rally.md distingue l'echec initial du resultat
apres separation. Ce succes partiel ne certifie pas toutes les cartes et
interactions du pack ; sa classification reste game-specific. Les points
5, 6, 28 et 33 restent ouverts dans corriger.txt.

Validation finale : engine:merge-contracts, 8 suites et 282 tests passes ;
14 tests du second jeu et 11 tests de la fabrique de compilation passes.
Typage, lint et quality:check passes, SDK et 39 jeux inclus. Logs :
logs/hazard-victory-{contracts,reuse-final,factory,types,lint,quality}.log.

## Continuation : diagnostics des packs narratif et de course (28)

Le pack choice-story-challenge designe maintenant tiles[index].type pour ses
bornes, puis le deuxieme element fautif pour les doublons de pions, jetons,
options numeriques, choix et cartes. Les chemins targetRules, optionRules et
decks utilisent le constructeur commun de proprietes : les points, crochets
et guillemets dans les cles ne changent plus le sens du chemin. Le pack
race-directional-hazards indique le membre precis de resources manquant.

Les tests par le compilateur public ont aussi revele que les factories de pions
et de cartes pouvaient echouer avant ces controles. La validation interne du
programme narratif est donc executee avant la construction des composants ;
la verification des ressources du document reste dans la validation contextuelle
ulterieure. Deux tests verifient explicitement que ces references restent rejetees.

18 nouveaux cas verifient les chemins extensions[0].config, la valeur received
et le maintien de la compilation du jeu original. Le point 28 reste ouvert pour
les autres validations specialisees et origines de composants non encore couvertes.
Les quatre points 5, 6, 28 et 33 restent dans corriger.txt.

Validation : 5 suites ciblees, 52 tests passes, dont 18 nouveaux diagnostics.
Typage, lint et quality:check passes, contrats des 39 jeux et SDK inclus.
Aucun plafond releve : 18760 lignes de production et 18684 de comportement.
Logs : logs/pack-diagnostics-{tests,final-tests,types,lint,quality}.log.

## Continuation : diagnostics de quatre packs de cartes (28)

Les packs cards-ordered-parade, cards-ritual-phases, collection-family-request
et collection-family-effects utilisent le diagnostic commun. Les doublons
designent le deuxieme identifiant ou la deuxieme valeur, les reponses de quiz
leur answerIndex, et les incoherences de famille leur family ou familyName.
La parade localise les valeurs inconnues et dupliquees dans sequence ; ses
recompenses et ressources scorees conservent les cles contenant des points.
Les effets manquants des cartes speciales rituelles designent cards[index].effects.
Les controles rituels passent avant la construction des composants pour eviter
qu'une erreur SDK sans provenance masque le diagnostic JSON.

Les erreurs portant sur une collection incomplete (aucune carte de famille,
type de carte absent, sequence incomplete) conservent la propriete collection :
il n'existe pas d'index correspondant a un element absent. Les erreurs concernant
un membre present designent ce membre. Les quatre jeux valides restent acceptes.
21 nouveaux tests du compilateur public couvrent ces cas.

Le point 28 reste ouvert pour les autres packs et la provenance non couverte.
Aucune suppression dans corriger.txt : les points 5, 6, 28 et 33 restent ouverts.

Validation : 3 suites ciblees, 79 tests passes (21 nouveaux cas, diagnostics
precedents et variantes des 39 jeux). Typage, lint et quality:check passes,
avec 76 tests de contrats finaux, SDK et registre synchronise. Plafonds inchanges :
18766 lignes de production, 18690 de comportement. Logs :
logs/card-diagnostics-{tests,regressions,types,lint,quality}.log.

## Continuation : diagnostics imbriques des courses (28)

Le pack race-resource-track localise maintenant chaque reference invalide dans
resources et mechanics : faceGains et ses ressources, dangerFaces, rerollValues,
tileRules.position/faces/gains/greaterResource/setCounter, extraDistance et ranking.
Une definition de gain absente designe sa propriete manquante, sans inventer
d'index. Les cles avec points, crochets ou guillemets sont correctement citees.

Les packs race-goose-track et race-bidirectional-collision designent les cases
invalides : type, backTo, numero n, bornes depart/arrivee. La reference au jeu
de pions de l'oie indique pawnSelection.setId. Les enveloppes de diagnostic de
race-paired-pawns et race-team-pawn-capture utilisent aussi le helper commun,
sans changer leurs regles ni leurs messages. Cette suppression de duplication
permet de conserver les plafonds de production existants.

26 nouveaux tests de compilation publique et les 21 tests precedents des cartes
passent. Les fixtures normalisees et clonees sont partagees par un helper de test ;
les donnees originales ne sont pas modifiees. Les cinq jeux valides compilent.
Le point 28 reste ouvert pour les autres validations specialisees ; les points
5, 6, 28 et 33 restent dans corriger.txt.

Validation finale : 47 tests cibles passes, dont 26 nouveaux ; typage, lint et
quality:check passes, avec les 76 tests de contrats finaux, les 39 jeux et le SDK.
Aucun plafond releve : 18768 lignes de production, 18692 de comportement.
Logs : logs/race-diagnostics-{tests,types,lint,quality}.log.

## Cloture des points 5, 6, 28 et 33

Point 5 : la matrice `second-game-catalogue.spec.ts` couvre les 38 cles du
registre avec une proposition de second objectif mecanique par pack. Elle
conserve les programmes TypeScript et les configurations du catalogue, puis
essaie dix manches suivies d'un classement. 37 contrats refusent cet objectif.
Le 38e compile mais termine a sa case d'arrivee : le test observe cette fin
prematuree et son rejeu. Les 38 classifications `game-specific` sont conservees.
Le rallye directionnel deja extrait conserve son second objectif fonctionnel
avec le mode explicite `external`. Les conceptions, limites et decisions sont
detaillees dans `second-game-catalogue.md`. Cette verification ne certifie pas
la reutilisabilite complete des 38 compositions.

Point 6 : trois capacites sont extraites de leurs consequences metier : calcul
du solde et de son deficit, consommation ordonnee d'une protection par statut ou
ressource, detection de collision. Le pack immobilier compose le calcul avec
sa politique de faillite ; le jeu narratif et la course composent le cout de
protection avec leurs effets ; trois courses composent la meme detection avec
des consequences differentes. La resolution de case directionnelle est aussi
isolee de la table des effets. L'ADR-010 inventorie ces capacites et celles deja
fournies par les composants (ownership, transfert, deplacement, statuts, choix,
branchement, pioche et resolution). Les regles metier restantes restent dans
leurs compositions specifiques. Une expedition independante execute protection,
cout et deficit sans extension, transforme le deficit en fatigue et passe son
rejeu. Aucun ajout de pack ni changement du SDK public.

Point 28 : les chemins des derniers diagnostics de victoire, de conflit entre
programmes, de ressources et de cartes jugees designent les champs source.
Une erreur dans un effet de carte genere pouvait encore sortir sans provenance :
la validation des effets se fait maintenant sur le contenu source avant leurs
copies dans les composants. Des mutations dans les 17 packs contenant ces effets
verifient le chemin extension/paquet/carte/effet/propriete et la valeur recue.
Les erreurs de main et de phases du pack juge designent aussi leur composant ou
phase precis. `engine:authoring-contracts` rend les suites de diagnostics,
provenance et authoring obligatoires dans le job d'architecture.

Point 33 : le ruleset GitHub 22038117 a ete modifie puis relu via l'API.
Il est actif, exige `quality` et la mise a jour de la branche avant fusion,
sans acteur de contournement. Les autres protections distantes sont conservees.
Les observations avant/apres sont dans `github-ruleset-before-required-quality.json`
et `github-ruleset-required-quality.json`. Le test CI verifie aussi la presence
des nouveaux essais de reutilisation et du gate de diagnostics. Les modifications
locales du workflow prendront effet lors de leur publication dans le depot.

La reconciliation du registre doit maintenant donner 36 points clos, zero ouvert.
Les preuves de cette continuation sont conservees dans `logs/remaining-*.log`.

Validation finale : 322 tests de contrats/rejeu dans 10 suites et 535 tests de
regression dans 35 suites passent (857 tests Jest). Le typage complet et le lint
complet passent. Le build compile 1898 fichiers et charge AppModule ; verify:dist
verifie 39 manifestes et 83 fichiers JSON de contenu. Le contrat SDK conserve
ses 111 declarations. Les packs restent sous leurs plafonds inchanges :
18759 lignes de production et 18683 lignes de comportement.

La cloture du dernier point a expose deux tests du backlog qui supposaient qu'un
point ouvert existait toujours. Les fixtures sont maintenant independantes de
l'etat courant et couvrent aussi zero point ouvert. Les dix tests maintiennent
le refus des numeros invalides, doublons, retraits sans mise a jour du registre
et clotures sans preuve. La reconciliation constate 36 clotures et zero point
ouvert. Le job CI d'architecture dispose de 20 minutes pour executer les gates
supplementaires de reutilisation et de diagnostics.

Le dernier `npm run quality:check` termine avec le code 0, y compris les
76 tests des contrats finaux, les controles SDK et les deux gates du backlog.
Log complet : `logs/remaining-quality-final.log`.
