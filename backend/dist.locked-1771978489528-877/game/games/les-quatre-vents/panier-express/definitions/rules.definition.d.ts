import { GameStateEntity } from '../../../../core/entities/game-state.entity';
export declare const PANIER_EXPRESS_PHASES: Array<{
    id: string;
    onEnter?: (s: GameStateEntity) => GameStateEntity;
}>;
