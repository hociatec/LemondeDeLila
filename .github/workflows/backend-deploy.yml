name: Backend - Build & Deploy

on:
  push:
    branches: [ "main" ]
  workflow_dispatch:

concurrency:
  group: backend-deploy-main
  cancel-in-progress: true

jobs:
  deploy:
    runs-on: [self-hosted, linux, x64]
    steps:
      - name: Deploy on server
        run: sudo /usr/local/sbin/deploy-lemonde-prod
