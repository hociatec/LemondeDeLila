name: Backend - Build & Deploy (manual)

on:
  workflow_dispatch:

jobs:
  deploy:
    runs-on: [self-hosted, Linux, X64]
    steps:
      - name: Deploy on server
        run: sudo /usr/local/sbin/deploy-lemonde-prod
