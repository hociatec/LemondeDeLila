<UserControl x:Class="client_win.Modules.Admin.Views.AdminView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="White">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- En-tête -->
        <Border Grid.Row="0" Background="#8B0000" Padding="20">
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="⚙️" FontSize="24" Margin="0,0,10,0" Foreground="White"/>
                <TextBlock Text="Panneau d'Administration"
                           FontSize="24"
                           FontWeight="Bold"
                           Foreground="White"/>
            </StackPanel>
        </Border>

        <!-- Contenu principal -->
        <Border Grid.Row="1" Padding="20">
            <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                <TextBlock Text="{Binding Status}"
                           FontSize="16"
                           Foreground="#555"
                           TextAlignment="Center"
                           TextWrapping="Wrap"
                           Margin="0,0,0,20"/>

                <!-- TODO: Ajouter outils d'administration -->
                <TextBlock Text="Les fonctionnalités suivantes seront ajoutées :"
                           FontSize="14"
                           Foreground="#888"
                           TextAlignment="Center"
                           Margin="0,10,0,5"/>
                <TextBlock FontSize="13" Foreground="#888" TextAlignment="Center" Margin="5">
                    <Run Text="• Gestion des utilisateurs"/>
                    <LineBreak/>
                    <Run Text="• Modération du contenu"/>
                    <LineBreak/>
                    <Run Text="• Consultation des logs système"/>
                    <LineBreak/>
                    <Run Text="• Statistiques et rapports"/>
                </TextBlock>
            </StackPanel>
        </Border>

        <!-- Pied avec bouton Fermer -->
        <Border Grid.Row="2" Background="#F5F5F5" Padding="20" BorderBrush="#DDD" BorderThickness="0,1,0,0">
            <Button Content="Fermer"
                    Command="{Binding CloseCommand}"
                    Width="120"
                    Height="35"
                    HorizontalAlignment="Right"
                    Background="#8B0000"
                    Foreground="White"
                    FontSize="14"
                    Cursor="Hand"/>
        </Border>
    </Grid>
</UserControl>
