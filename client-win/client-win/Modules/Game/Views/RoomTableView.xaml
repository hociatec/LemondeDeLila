<UserControl x:Class="client_win.Modules.Game.Views.RoomTableView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Loaded="OnLoaded"
             Unloaded="OnUnloaded"
             PreviewKeyDown="OnPreviewKeyDown">
    <Grid Background="#0F1926">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="#13263B" Padding="16">
            <StackPanel>
                <TextBlock Text="{Binding RoomName}" FontSize="22" FontWeight="SemiBold" Foreground="White"/>
                <StackPanel Orientation="Horizontal" Margin="0,6,0,0">
                    <TextBlock Text="{Binding GameType, StringFormat=Jeu: {0}}"
                               Foreground="#9CB2D1" Margin="0,0,12,0"/>
                    <TextBlock Text="{Binding PrivacyLabel, StringFormat=Confidentialite: {0}}"
                               Foreground="#9CB2D1" Margin="0,0,12,0"/>
                    <TextBlock Text="{Binding RoleLabel, StringFormat=Role: {0}}"
                               Foreground="#9CB2D1"/>
                </StackPanel>
                <TextBlock Text="{Binding CountsLabel}" Foreground="#9CB2D1" Margin="0,4,0,0"/>
            </StackPanel>
        </Border>

        <Grid Grid.Row="1" Margin="16">
            <!-- Zone de jeu principale (l'historique est un panneau qui apparaît/disparaît) -->
            <Border x:Name="GamePanel"
                    Background="#101C2B"
                    CornerRadius="10"
                    Padding="16"
                    Focusable="True"
                    KeyboardNavigation.TabNavigation="None"
                    KeyboardNavigation.IsTabStop="True">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <!-- Panneau d'aide à l'ouverture de table -->
                    <Border Background="#0B1523" CornerRadius="10" Padding="14">
                        <StackPanel>
                            <TextBlock Text="Table créée" Foreground="White" FontSize="18" FontWeight="SemiBold" />
                            <TextBlock Text="Tab : historique (bascule) • Shift+Tab : retour (bascule)"
                                       Foreground="#9CB2D1" Margin="0,6,0,0"/>
                            <TextBlock Text="Q : quitter • Ctrl+H : public/privé • Ctrl+C : joueur/spectateur"
                                       Foreground="#9CB2D1" Margin="0,2,0,0"/>
                        </StackPanel>
                    </Border>

                    <!-- Fenêtre de jeu -->
                    <Border Grid.Row="1" Background="#0E1724" CornerRadius="10" Margin="0,12,0,0">
                        <Grid>
                            <TextBlock Text="Fenêtre de jeu (WIP)"
                                       Foreground="#9CB2D1"
                                       HorizontalAlignment="Center"
                                       VerticalAlignment="Center"
                                       FontSize="16"/>
                        </Grid>
                    </Border>
                </Grid>
            </Border>

            <!-- Panneau historique (toggle via Tab / Shift+Tab) -->
            <Border x:Name="HistoryPanel"
                    Background="#101C2B"
                    CornerRadius="10"
                    Padding="12"
                    Width="360"
                    HorizontalAlignment="Left"
                    VerticalAlignment="Stretch"
                    Margin="0"
                    Visibility="Collapsed"
                    Focusable="False"
                    Panel.ZIndex="10">
                <StackPanel>
                    <TextBlock Text="Historique" Foreground="White" FontWeight="SemiBold" Margin="0,0,0,8"/>
                    <ListBox x:Name="HistoryList"
                             ItemsSource="{Binding History}"
                             Background="#0B1523"
                             Foreground="White"
                             VirtualizingStackPanel.IsVirtualizing="False"
                             KeyboardNavigation.TabNavigation="None"
                             IsTabStop="True" />
                </StackPanel>
            </Border>
        </Grid>

        <Border Grid.Row="2" Background="#0B1523" Padding="10">
            <TextBlock Text="{Binding Status}" Foreground="#F2C14E"/>
        </Border>
    </Grid>
</UserControl>
