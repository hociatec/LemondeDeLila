<UserControl x:Class="client_win.Modules.Shell.Views.StableContentHost"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             x:Name="Self"
             Focusable="True"
             IsTabStop="False">
    <Grid>
        <!-- Keep the previous view alive until the new one is ready + focused.
             This avoids NVDA "indisponible" caused by focusing an element that vanishes during a swap. -->
        <ContentPresenter x:Name="PreviousPresenter"
                          Content="{Binding PreviousContent, ElementName=Self}"
                          Focusable="False"
                          IsHitTestVisible="False"
                          Panel.ZIndex="0">
            <ContentPresenter.Style>
                <Style TargetType="ContentPresenter">
                    <Setter Property="Visibility" Value="Visible" />
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding PreviousContent, ElementName=Self}" Value="{x:Null}">
                            <Setter Property="Visibility" Value="Collapsed" />
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </ContentPresenter.Style>
        </ContentPresenter>

        <ContentPresenter x:Name="CurrentPresenter"
                          Content="{Binding CurrentContent, ElementName=Self}"
                          Panel.ZIndex="1" />
    </Grid>
</UserControl>
