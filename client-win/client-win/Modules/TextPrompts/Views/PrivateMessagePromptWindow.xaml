<Window x:Class="client_win.Modules.TextPrompts.Views.PrivateMessagePromptWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        WindowStartupLocation="CenterOwner"
        SizeToContent="WidthAndHeight"
        MinWidth="520"
        Background="#0B1523"
        Foreground="White"
        Title="{Binding Title}"
        PreviewKeyDown="OnPreviewKeyDown">
    <Border Background="#12263C" CornerRadius="12" Padding="18" Margin="18">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <TextBlock Text="{Binding SubjectLabel}"
                       FontSize="16"
                       FontWeight="SemiBold"
                       Margin="0,0,0,10" />

            <TextBox Grid.Row="1"
                     x:Name="SubjectBox"
                     Text="{Binding Subject, UpdateSourceTrigger=PropertyChanged}"
                     MinWidth="460"
                     MinHeight="34"
                     Background="#0C1523"
                     Foreground="White"
                     BorderBrush="#284C75"
                     BorderThickness="1"
                     Padding="10"
                     TextWrapping="Wrap"
                     AcceptsReturn="False"
                     AutomationProperties.Name="{Binding SubjectLabel}" />

            <TextBlock Grid.Row="2"
                       Text="{Binding MessageLabel}"
                       FontSize="16"
                       FontWeight="SemiBold"
                       Margin="0,12,0,10" />

            <TextBox Grid.Row="3"
                     x:Name="MessageBox"
                     Text="{Binding Message, UpdateSourceTrigger=PropertyChanged}"
                     MinWidth="460"
                     MinHeight="140"
                     Background="#0C1523"
                     Foreground="White"
                     BorderBrush="#284C75"
                     BorderThickness="1"
                     Padding="10"
                     TextWrapping="Wrap"
                     AcceptsReturn="True"
                     VerticalScrollBarVisibility="Auto"
                     HorizontalScrollBarVisibility="Disabled"
                     AutomationProperties.Name="{Binding MessageLabel}" />

            <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,12,0,0">
                <Button Content="Annuler" Width="110" Margin="0,0,8,0" Click="OnCancelClick" />
                <Button Content="Envoyer" Width="110" IsDefault="True" Click="OnOkClick" />
            </StackPanel>
        </Grid>
    </Border>
</Window>

