#pragma once

// Compatibility include retained for downstream code. New presentation code
// must use NavigationController directly.
#include "shared/accessibility/application/NavigationController.h"
